(function(){"use strict";(function(e,r){try{if(typeof document<"u"){var o=document.createElement("style");o.setAttribute("data-ctc-connector-styles","");for(const t in r.attributes)o.setAttribute(t,r.attributes[t]);o.appendChild(document.createTextNode(e)),document.head.appendChild(o)}}catch(t){console.error("vite-plugin-css-injected-by-js",t)}})('._paymentForm_1jvri_36{margin-top:1rem}._row_1jvri_46{display:flex;flex-direction:row;gap:1rem;position:relative}._wrapper_1jvri_40 *{font-family:Roboto,sans-serif;box-sizing:border-box}._twoColumnLayout_1jvri_65{display:flex;flex-flow:row wrap;column-gap:1rem}._twoColumnLayout_1jvri_65>*{flex:1 0 150px}._container_1jvri_74{margin-top:1rem}._container_1jvri_74 iframe{height:3.317rem!important;border:1px solid #949494!important;border-radius:.25rem;padding-left:1rem;width:100%;float:none!important}._container_1jvri_74 ._input_1jvri_85{display:inline-block;position:relative;width:100%;margin-bottom:1rem}._container_1jvri_74 ._error_1jvri_91{color:#d32f2f;font-size:.75rem;padding:0 .75rem .25rem 1rem;position:rela2tive}.is-valid iframe{border:1px solid #949494!important}.is-onfocus iframe{border:2px solid #186ec3!important}.is-onfocus~._error_1jvri_91{display:none}.is-invalid iframe,._inputError_1jvri_111 :not(.is-onfocus) iframe,._inputErrorEmptyText_1jvri_112 iframe{border:2px solid #d32f2f!important}._floatingLabel_1jvri_117{position:absolute;pointer-events:none;left:.8rem;top:1rem;transition:.2s ease all;background-color:#fff;padding:0 .25rem;width:80%}._wrapper_1jvri_40{position:relative;width:100%;margin-bottom:1rem}._cardIcons_1jvri_134{display:none}.is-empty~._cardIcons_1jvri_134{display:block}.is-empty~._floatingCard_1jvri_142{display:none}.is-onfocus~._floatingLabel_1jvri_117,.is-valid~._floatingLabel_1jvri_117,.is-invalid:not(._inputEmpty_1jvri_148)~._floatingLabel_1jvri_117{top:-.5rem;font-size:.75rem;width:auto}.is-valid~._floatingLabel_1jvri_117{color:#949494}.is-onfocus~._floatingLabel_1jvri_117{color:#186ec3}.is-invalid:not(._inputEmpty_1jvri_148)~._floatingLabel_1jvri_117{color:#d32f2f}._inputErrorEmptyText_1jvri_112~._floatingLabel_1jvri_117{color:#000}._floatingCard_1jvri_142{position:absolute!important;pointer-events:none;right:0;top:0;transition:.2s ease all;background-color:#fff;padding:0 .25rem}._subHeading_1jvri_53{color:#5e6368;font-size:.8125rem;margin-top:.125rem;margin-bottom:1.5rem}._alert_1jvri_189{margin:1em 0}._cardRow_1jvri_193{display:flex;gap:.25rem;margin-top:.5rem}._cardIcon_1jvri_134{border:1px solid #d9d9d9;border-radius:.156rem}._hidden_1jvri_204{display:none}:root,:host{--ctc-button: #186ec3;--ctc-button-hover: color-mix(in srgb, var(--ctc-button), black 15%);--ctc-button-disabled: #e0e0e0;--ctc-button-text: #fff;--ctc-button-disabled-text: #a2a3a4}:root,:host{--ctc-radio: #186ec3}:root,:host{--ctc-checkbox: #186ec3}:root,:host{--ctc-input-field-focus: #186ec3}:root,:host{--ctc-font-family: "Roboto", sans-serif}@supports not (background: color-mix(in srgb,red 50%,blue)){:root{--ctc-button-hover: var(--ctc-button)}}._button_1eyp1_41{color:var(--ctc-button-text);padding:.5rem 1.375rem;background-color:var(--ctc-button);border:0 none;border-radius:.25rem;font-size:.9375rem;font-weight:400;font-family:var(--ctc-font-family);text-transform:uppercase;line-height:1.5rem;letter-spacing:.43px;box-shadow:0 2px 2px #00000024,0 3px 1px -2px #0000001f,0 1px 5px #0003;background-position:center;transition:background-color .8s;cursor:pointer}._button_1eyp1_41:hover{background:var(--ctc-button-hover) radial-gradient(circle,transparent 1%,var(--ctc-button-hover) 1%) center/15000%}._button_1eyp1_41:active{background-color:var(--ctc-button-hover);background-size:100%;transition:background-color 0s}._button_1eyp1_41:disabled{color:var(--ctc-button-disabled-text);background-color:var(--ctc-button-disabled);pointer-events:none;box-shadow:none}._button_1eyp1_41:disabled:hover,._button_1eyp1_41:disabled:active{background-color:var(--ctc-button-disabled);cursor:not-allowed}._fullWidth_1eyp1_77{width:100%}._linkButton_1eyp1_81{text-decoration:none;text-transform:unset;background:none;border-bottom:1.5px solid transparent;box-shadow:none;padding:0;border-radius:0;color:var(--ctc-button);font-size:1rem;font-weight:700;line-height:1.188rem;letter-spacing:.009rem}._linkButton_1eyp1_81:hover,._linkButton_1eyp1_81:active{cursor:pointer;border-bottom:1.5px solid var(--ctc-button);background:none}._linkButton_1eyp1_81._disabled_1eyp1_100{color:#a2a3a4;text-decoration:none;cursor:not-allowed;pointer-events:none;background-color:transparent;box-shadow:none}._linkButton_1eyp1_81._disabled_1eyp1_100:hover,._linkButton_1eyp1_81._disabled_1eyp1_100:active{border-bottom:none}._lowOpacityButton_1eyp1_112{background-color:color-mix(in srgb,var(--ctc-button),transparent 30%)}@supports not (background: color-mix(in srgb,red 50%,blue)){._lowOpacityButton_1eyp1_112{background-color:var(--ctc-button);opacity:60%}}._textButton_1eyp1_122{background-color:transparent;color:var(--ctc-button);box-shadow:none;padding:.5rem .6875rem}._textButton_1eyp1_122:hover{background:color-mix(in srgb,var(--ctc-button),transparent 90%) radial-gradient(circle,transparent 1%,color-mix(in srgb,var(--ctc-button),transparent 90%) 1%) center/15000%}._textButton_1eyp1_122:active{background-color:color-mix(in srgb,var(--ctc-button),transparent 90%);background-size:100%;transition:background-color 0s}._textButton_1eyp1_122:focus{background-color:color-mix(in srgb,var(--ctc-button),transparent 90%)}._textButton_1eyp1_122:disabled{background-color:transparent;color:#e0e0e0}._errorButton_1eyp1_144{color:#b52323;background:transparent;border:1px solid transparent;transition:none;box-shadow:none;outline:none}._errorButton_1eyp1_144:hover{background:#fbdada}._errorButton_1eyp1_144:active{border:1px solid #b52323;background:#ffc5c5}._errorButton_1eyp1_144:disabled{background-color:transparent;color:#e0e0e0}',{})})();
var Ke = Object.defineProperty;
var Me = (i, d, c) => d in i ? Ke(i, d, { enumerable: !0, configurable: !0, writable: !0, value: c }) : i[d] = c;
var P = (i, d, c) => Me(i, typeof d != "symbol" ? d + "" : d, c);
var q = /* @__PURE__ */ ((i) => (i.applepay = "applepay", i.bancontactcard = "bcmc", i.eps = "eps", i.googlepay = "googlepay", i.ideal = "ideal", i.invoice = "invoice", i.paypal = "paypal", i.prepayment = "prepayment", i.GuaranteedInvoice = "GuaranteedInvoice", i.GuaranteedSepa = "GuaranteedSepa", i.twint = "twint", i.sepa = "sepa", i.ach = "ach", i.creditcard = "creditcard", i.onlinebanktransfer = "onlinebanktransfer", i.alipay = "alipay", i.bancontact = "bancontact", i.blik = "blik", i.mbway = "mbway", i.multibanco = "multibanco", i.postfinance = "postfinance", i.postfinancecard = "postfinancecard", i.przelewy24 = "przelewy24", i.trustly = "trustly", i.wechatpay = "wechatpay", i))(q || {});
class M {
  constructor(d, c, e) {
    P(this, "paymentMethod");
    P(this, "sdk");
    P(this, "processorUrl");
    P(this, "sessionId");
    P(this, "environment");
    P(this, "onComplete");
    P(this, "onError");
    P(this, "clientKey", "");
    P(this, "paymentCompleted", !1);
    P(this, "commercetoolsPaymentId", null);
    this.paymentMethod = d, this.sdk = c.sdk, this.processorUrl = c.processorUrl, this.sessionId = c.sessionId, this.environment = c.environment, this.onComplete = c.onComplete, this.onError = c.onError;
  }
  completePayment(d) {
    this.paymentCompleted || (this.paymentCompleted = !0, this.onComplete(d));
  }
}
const xe = "_wrapper_1jvri_40", O = {
  wrapper: xe
}, We = "_button_1eyp1_41", ze = "_fullWidth_1eyp1_77", E = {
  button: We,
  fullWidth: ze
};
var Ne = {}, $e;
function Xe() {
  return $e || ($e = 1, function(i) {
    Object.defineProperty(i, "__esModule", { value: !0 }), i.Type = i.StandardType = i.ExtendedTypeBuilder = i.StandardTypeBuilder = i.TypeBuilder = i.TemplateLiteralGenerator = i.TemplateLiteralFinite = i.TemplateLiteralParser = i.TemplateLiteralParserError = i.TemplateLiteralResolver = i.TemplateLiteralPattern = i.KeyResolver = i.ObjectMap = i.TypeClone = i.TypeExtends = i.TypeExtendsResult = i.ExtendsUndefined = i.TypeGuard = i.TypeGuardUnknownTypeError = i.FormatRegistry = i.TypeRegistry = i.PatternStringExact = i.PatternNumberExact = i.PatternBooleanExact = i.PatternString = i.PatternNumber = i.PatternBoolean = i.Kind = i.Hint = i.Modifier = void 0, i.Modifier = Symbol.for("TypeBox.Modifier"), i.Hint = Symbol.for("TypeBox.Hint"), i.Kind = Symbol.for("TypeBox.Kind"), i.PatternBoolean = "(true|false)", i.PatternNumber = "(0|[1-9][0-9]*)", i.PatternString = "(.*)", i.PatternBooleanExact = `^${i.PatternBoolean}$`, i.PatternNumberExact = `^${i.PatternNumber}$`, i.PatternStringExact = `^${i.PatternString}$`;
    var d;
    (function(w) {
      const a = /* @__PURE__ */ new Map();
      function l() {
        return new Map(a);
      }
      w.Entries = l;
      function y() {
        return a.clear();
      }
      w.Clear = y;
      function f(m) {
        return a.has(m);
      }
      w.Has = f;
      function p(m, B) {
        a.set(m, B);
      }
      w.Set = p;
      function T(m) {
        return a.get(m);
      }
      w.Get = T;
    })(d = i.TypeRegistry || (i.TypeRegistry = {})), function(w) {
      const a = /* @__PURE__ */ new Map();
      function l() {
        return new Map(a);
      }
      w.Entries = l;
      function y() {
        return a.clear();
      }
      w.Clear = y;
      function f(m) {
        return a.has(m);
      }
      w.Has = f;
      function p(m, B) {
        a.set(m, B);
      }
      w.Set = p;
      function T(m) {
        return a.get(m);
      }
      w.Get = T;
    }(i.FormatRegistry || (i.FormatRegistry = {}));
    class c extends Error {
      constructor(a) {
        super("TypeGuard: Unknown type"), this.schema = a;
      }
    }
    i.TypeGuardUnknownTypeError = c;
    var e;
    (function(w) {
      function a(n) {
        return typeof n == "object" && n !== null && !Array.isArray(n);
      }
      function l(n) {
        return typeof n == "object" && n !== null && Array.isArray(n);
      }
      function y(n) {
        try {
          return new RegExp(n), !0;
        } catch {
          return !1;
        }
      }
      function f(n) {
        if (typeof n != "string")
          return !1;
        for (let j = 0; j < n.length; j++) {
          const N = n.charCodeAt(j);
          if (N >= 7 && N <= 13 || N === 27 || N === 127)
            return !1;
        }
        return !0;
      }
      function p(n) {
        return typeof n == "bigint";
      }
      function T(n) {
        return typeof n == "string";
      }
      function m(n) {
        return typeof n == "number" && globalThis.Number.isFinite(n);
      }
      function B(n) {
        return typeof n == "boolean";
      }
      function C(n) {
        return n === void 0 || n !== void 0 && p(n);
      }
      function I(n) {
        return n === void 0 || n !== void 0 && m(n);
      }
      function L(n) {
        return n === void 0 || n !== void 0 && B(n);
      }
      function U(n) {
        return n === void 0 || n !== void 0 && T(n);
      }
      function S(n) {
        return n === void 0 || n !== void 0 && T(n) && f(n) && y(n);
      }
      function v(n) {
        return n === void 0 || n !== void 0 && T(n) && f(n);
      }
      function $(n) {
        return n === void 0 || V(n);
      }
      function W(n) {
        return g(n) && n[i.Kind] === "Any" && U(n.$id);
      }
      w.TAny = W;
      function k(n) {
        return g(n) && n[i.Kind] === "Array" && n.type === "array" && U(n.$id) && V(n.items) && I(n.minItems) && I(n.maxItems) && L(n.uniqueItems);
      }
      w.TArray = k;
      function A(n) {
        return g(n) && n[i.Kind] === "BigInt" && n.type === "null" && n.typeOf === "BigInt" && U(n.$id) && C(n.multipleOf) && C(n.minimum) && C(n.maximum) && C(n.exclusiveMinimum) && C(n.exclusiveMaximum);
      }
      w.TBigInt = A;
      function K(n) {
        return g(n) && n[i.Kind] === "Boolean" && n.type === "boolean" && U(n.$id);
      }
      w.TBoolean = K;
      function D(n) {
        if (!(g(n) && n[i.Kind] === "Constructor" && n.type === "object" && n.instanceOf === "Constructor" && U(n.$id) && l(n.parameters) && V(n.returns)))
          return !1;
        for (const j of n.parameters)
          if (!V(j))
            return !1;
        return !0;
      }
      w.TConstructor = D;
      function Te(n) {
        return g(n) && n[i.Kind] === "Date" && n.type === "object" && n.instanceOf === "Date" && U(n.$id) && I(n.minimumTimestamp) && I(n.maximumTimestamp) && I(n.exclusiveMinimumTimestamp) && I(n.exclusiveMaximumTimestamp);
      }
      w.TDate = Te;
      function me(n) {
        if (!(g(n) && n[i.Kind] === "Function" && n.type === "object" && n.instanceOf === "Function" && U(n.$id) && l(n.parameters) && V(n.returns)))
          return !1;
        for (const j of n.parameters)
          if (!V(j))
            return !1;
        return !0;
      }
      w.TFunction = me;
      function ce(n) {
        return g(n) && n[i.Kind] === "Integer" && n.type === "integer" && U(n.$id) && I(n.multipleOf) && I(n.minimum) && I(n.maximum) && I(n.exclusiveMinimum) && I(n.exclusiveMaximum);
      }
      w.TInteger = ce;
      function be(n) {
        if (!(g(n) && n[i.Kind] === "Intersect" && l(n.allOf) && U(n.type) && (L(n.unevaluatedProperties) || $(n.unevaluatedProperties)) && U(n.$id)) || "type" in n && n.type !== "object")
          return !1;
        for (const j of n.allOf)
          if (!V(j))
            return !1;
        return !0;
      }
      w.TIntersect = be;
      function g(n) {
        return a(n) && i.Kind in n && typeof n[i.Kind] == "string";
      }
      w.TKind = g;
      function ae(n) {
        return g(n) && n[i.Kind] === "Literal" && U(n.$id) && (T(n.const) || m(n.const) || B(n.const) || p(n.const));
      }
      w.TLiteral = ae;
      function le(n) {
        return g(n) && n[i.Kind] === "Never" && a(n.not) && globalThis.Object.getOwnPropertyNames(n.not).length === 0;
      }
      w.TNever = le;
      function se(n) {
        return g(n) && n[i.Kind] === "Not" && l(n.allOf) && n.allOf.length === 2 && a(n.allOf[0]) && V(n.allOf[0].not) && V(n.allOf[1]);
      }
      w.TNot = se;
      function de(n) {
        return g(n) && n[i.Kind] === "Null" && n.type === "null" && U(n.$id);
      }
      w.TNull = de;
      function we(n) {
        return g(n) && n[i.Kind] === "Number" && n.type === "number" && U(n.$id) && I(n.multipleOf) && I(n.minimum) && I(n.maximum) && I(n.exclusiveMinimum) && I(n.exclusiveMaximum);
      }
      w.TNumber = we;
      function he(n) {
        if (!(g(n) && n[i.Kind] === "Object" && n.type === "object" && U(n.$id) && a(n.properties) && (L(n.additionalProperties) || $(n.additionalProperties)) && I(n.minProperties) && I(n.maxProperties)))
          return !1;
        for (const [j, N] of Object.entries(n.properties))
          if (!f(j) || !V(N))
            return !1;
        return !0;
      }
      w.TObject = he;
      function Pe(n) {
        return g(n) && n[i.Kind] === "Promise" && n.type === "object" && n.instanceOf === "Promise" && U(n.$id) && V(n.item);
      }
      w.TPromise = Pe;
      function Se(n) {
        if (!(g(n) && n[i.Kind] === "Record" && n.type === "object" && U(n.$id) && n.additionalProperties === !1 && a(n.patternProperties)))
          return !1;
        const j = Object.keys(n.patternProperties);
        return !(j.length !== 1 || !y(j[0]) || !V(n.patternProperties[j[0]]));
      }
      w.TRecord = Se;
      function Be(n) {
        return g(n) && n[i.Kind] === "Ref" && U(n.$id) && T(n.$ref);
      }
      w.TRef = Be;
      function re(n) {
        return g(n) && n[i.Kind] === "String" && n.type === "string" && U(n.$id) && I(n.minLength) && I(n.maxLength) && S(n.pattern) && v(n.format);
      }
      w.TString = re;
      function ve(n) {
        return g(n) && n[i.Kind] === "Symbol" && n.type === "null" && n.typeOf === "Symbol" && U(n.$id);
      }
      w.TSymbol = ve;
      function pe(n) {
        return g(n) && n[i.Kind] === "TemplateLiteral" && n.type === "string" && T(n.pattern) && n.pattern[0] === "^" && n.pattern[n.pattern.length - 1] === "$";
      }
      w.TTemplateLiteral = pe;
      function z(n) {
        return g(n) && n[i.Kind] === "This" && U(n.$id) && T(n.$ref);
      }
      w.TThis = z;
      function ge(n) {
        if (!(g(n) && n[i.Kind] === "Tuple" && n.type === "array" && U(n.$id) && m(n.minItems) && m(n.maxItems) && n.minItems === n.maxItems))
          return !1;
        if (n.items === void 0 && n.additionalItems === void 0 && n.minItems === 0)
          return !0;
        if (!l(n.items))
          return !1;
        for (const j of n.items)
          if (!V(j))
            return !1;
        return !0;
      }
      w.TTuple = ge;
      function Ee(n) {
        return g(n) && n[i.Kind] === "Undefined" && n.type === "null" && n.typeOf === "Undefined" && U(n.$id);
      }
      w.TUndefined = Ee;
      function ie(n) {
        if (!(g(n) && n[i.Kind] === "Union" && l(n.anyOf) && U(n.$id)))
          return !1;
        for (const j of n.anyOf)
          if (!V(j))
            return !1;
        return !0;
      }
      w.TUnion = ie;
      function fe(n) {
        return ie(n) && n.anyOf.every((j) => ae(j) && typeof j.const == "string");
      }
      w.TUnionLiteral = fe;
      function G(n) {
        return g(n) && n[i.Kind] === "Uint8Array" && n.type === "object" && U(n.$id) && n.instanceOf === "Uint8Array" && I(n.minByteLength) && I(n.maxByteLength);
      }
      w.TUint8Array = G;
      function Oe(n) {
        return g(n) && n[i.Kind] === "Unknown" && U(n.$id);
      }
      w.TUnknown = Oe;
      function ye(n) {
        return g(n) && n[i.Kind] === "Unsafe";
      }
      w.TUnsafe = ye;
      function Ce(n) {
        return g(n) && n[i.Kind] === "Void" && n.type === "null" && n.typeOf === "Void" && U(n.$id);
      }
      w.TVoid = Ce;
      function Ue(n) {
        return a(n) && n[i.Modifier] === "ReadonlyOptional";
      }
      w.TReadonlyOptional = Ue;
      function Re(n) {
        return a(n) && n[i.Modifier] === "Readonly";
      }
      w.TReadonly = Re;
      function Le(n) {
        return a(n) && n[i.Modifier] === "Optional";
      }
      w.TOptional = Le;
      function V(n) {
        return typeof n == "object" && (W(n) || k(n) || K(n) || A(n) || D(n) || Te(n) || me(n) || ce(n) || be(n) || ae(n) || le(n) || se(n) || de(n) || we(n) || he(n) || Pe(n) || Se(n) || Be(n) || re(n) || ve(n) || pe(n) || z(n) || ge(n) || Ee(n) || ie(n) || G(n) || Oe(n) || ye(n) || Ce(n) || g(n) && d.Has(n[i.Kind]));
      }
      w.TSchema = V;
    })(e = i.TypeGuard || (i.TypeGuard = {})), function(w) {
      function a(l) {
        return l[i.Kind] === "Undefined" ? !0 : l[i.Kind] === "Union" ? l.anyOf.some((f) => a(f)) : !1;
      }
      w.Check = a;
    }(i.ExtendsUndefined || (i.ExtendsUndefined = {}));
    var o;
    (function(w) {
      w[w.Union = 0] = "Union", w[w.True = 1] = "True", w[w.False = 2] = "False";
    })(o = i.TypeExtendsResult || (i.TypeExtendsResult = {}));
    var s;
    (function(w) {
      function a(r) {
        return r === o.False ? o.False : o.True;
      }
      function l(r, t) {
        return o.True;
      }
      function y(r, t) {
        return e.TIntersect(t) ? v(r, t) : e.TUnion(t) && t.anyOf.some((R) => e.TAny(R) || e.TUnknown(R)) ? o.True : e.TUnion(t) ? o.Union : e.TUnknown(t) || e.TAny(t) ? o.True : o.Union;
      }
      function f(r, t) {
        return e.TUnknown(r) ? o.False : e.TAny(r) ? o.Union : e.TNever(r) ? o.True : o.False;
      }
      function p(r, t) {
        return e.TIntersect(t) ? v(r, t) : e.TUnion(t) ? N(r, t) : e.TUnknown(t) ? X() : e.TAny(t) ? l() : e.TObject(t) && re(t) ? o.True : e.TArray(t) ? a(_(r.items, t.items)) : o.False;
      }
      function T(r, t) {
        return e.TIntersect(t) ? v(r, t) : e.TUnion(t) ? N(r, t) : e.TNever(t) ? D() : e.TUnknown(t) ? X() : e.TAny(t) ? l() : e.TObject(t) ? z(r, t) : e.TRecord(t) ? G(r, t) : e.TBigInt(t) ? o.True : o.False;
      }
      function m(r, t) {
        return e.TLiteral(r) && typeof r.const == "boolean" || e.TBoolean(r) ? o.True : o.False;
      }
      function B(r, t) {
        return e.TIntersect(t) ? v(r, t) : e.TUnion(t) ? N(r, t) : e.TNever(t) ? D() : e.TUnknown(t) ? X() : e.TAny(t) ? l() : e.TObject(t) ? z(r, t) : e.TRecord(t) ? G(r, t) : e.TBoolean(t) ? o.True : o.False;
      }
      function C(r, t) {
        return e.TIntersect(t) ? v(r, t) : e.TUnion(t) ? N(r, t) : e.TUnknown(t) ? X() : e.TAny(t) ? l() : e.TObject(t) ? z(r, t) : !e.TConstructor(t) || r.parameters.length > t.parameters.length || !r.parameters.every((R, ne) => a(_(t.parameters[ne], R)) === o.True) ? o.False : a(_(r.returns, t.returns));
      }
      function I(r, t) {
        return e.TIntersect(t) ? v(r, t) : e.TUnion(t) ? N(r, t) : e.TUnknown(t) ? X() : e.TAny(t) ? l() : e.TObject(t) ? z(r, t) : e.TRecord(t) ? G(r, t) : e.TDate(t) ? o.True : o.False;
      }
      function L(r, t) {
        return e.TIntersect(t) ? v(r, t) : e.TUnion(t) ? N(r, t) : e.TUnknown(t) ? X() : e.TAny(t) ? l() : e.TObject(t) ? z(r, t) : !e.TFunction(t) || r.parameters.length > t.parameters.length || !r.parameters.every((R, ne) => a(_(t.parameters[ne], R)) === o.True) ? o.False : a(_(r.returns, t.returns));
      }
      function U(r, t) {
        return e.TLiteral(r) && typeof r.const == "number" || e.TNumber(r) || e.TInteger(r) ? o.True : o.False;
      }
      function S(r, t) {
        return e.TIntersect(t) ? v(r, t) : e.TUnion(t) ? N(r, t) : e.TNever(t) ? D() : e.TUnknown(t) ? X() : e.TAny(t) ? l() : e.TObject(t) ? z(r, t) : e.TRecord(t) ? G(r, t) : e.TInteger(t) || e.TNumber(t) ? o.True : o.False;
      }
      function v(r, t) {
        return t.allOf.every((R) => _(r, R) === o.True) ? o.True : o.False;
      }
      function $(r, t) {
        return r.allOf.some((R) => _(R, t) === o.True) ? o.True : o.False;
      }
      function W(r) {
        return typeof r.const == "string";
      }
      function k(r) {
        return typeof r.const == "number";
      }
      function A(r) {
        return typeof r.const == "boolean";
      }
      function K(r, t) {
        return e.TIntersect(t) ? v(r, t) : e.TUnion(t) ? N(r, t) : e.TNever(t) ? D() : e.TUnknown(t) ? X() : e.TAny(t) ? l() : e.TObject(t) ? z(r, t) : e.TRecord(t) ? G(r, t) : e.TString(t) ? ye(r) : e.TNumber(t) ? ce(r) : e.TInteger(t) ? U(r) : e.TBoolean(t) ? m(r) : e.TLiteral(t) && t.const === r.const ? o.True : o.False;
      }
      function D(r, t) {
        return o.False;
      }
      function Te(r, t) {
        return o.True;
      }
      function me(r, t) {
        return e.TIntersect(t) ? v(r, t) : e.TUnion(t) ? N(r, t) : e.TNever(t) ? D() : e.TUnknown(t) ? X() : e.TAny(t) ? l() : e.TObject(t) ? z(r, t) : e.TRecord(t) ? G(r, t) : e.TNull(t) ? o.True : o.False;
      }
      function ce(r, t) {
        return e.TLiteral(r) && k(r) || e.TNumber(r) || e.TInteger(r) ? o.True : o.False;
      }
      function be(r, t) {
        return e.TIntersect(t) ? v(r, t) : e.TUnion(t) ? N(r, t) : e.TNever(t) ? D() : e.TUnknown(t) ? X() : e.TAny(t) ? l() : e.TObject(t) ? z(r, t) : e.TRecord(t) ? G(r, t) : e.TInteger(t) || e.TNumber(t) ? o.True : o.False;
      }
      function g(r, t) {
        return globalThis.Object.keys(r.properties).length === t;
      }
      function ae(r) {
        return re(r);
      }
      function le(r) {
        return g(r, 0) || g(r, 1) && "description" in r.properties && e.TUnion(r.properties.description) && r.properties.description.anyOf.length === 2 && (e.TString(r.properties.description.anyOf[0]) && e.TUndefined(r.properties.description.anyOf[1]) || e.TString(r.properties.description.anyOf[1]) && e.TUndefined(r.properties.description.anyOf[0]));
      }
      function se(r) {
        return g(r, 0);
      }
      function de(r) {
        return g(r, 0);
      }
      function we(r) {
        return g(r, 0);
      }
      function he(r) {
        return g(r, 0);
      }
      function Pe(r) {
        return re(r);
      }
      function Se(r) {
        const t = i.Type.Number();
        return g(r, 0) || g(r, 1) && "length" in r.properties && a(_(r.properties.length, t)) === o.True;
      }
      function Be(r) {
        return g(r, 0);
      }
      function re(r) {
        const t = i.Type.Number();
        return g(r, 0) || g(r, 1) && "length" in r.properties && a(_(r.properties.length, t)) === o.True;
      }
      function ve(r) {
        const t = i.Type.Function([i.Type.Any()], i.Type.Any());
        return g(r, 0) || g(r, 1) && "then" in r.properties && a(_(r.properties.then, t)) === o.True;
      }
      function pe(r, t) {
        return _(r, t) === o.False || e.TOptional(r) && !e.TOptional(t) ? o.False : o.True;
      }
      function z(r, t) {
        return e.TUnknown(r) ? o.False : e.TAny(r) ? o.Union : e.TNever(r) || e.TLiteral(r) && W(r) && ae(t) || e.TLiteral(r) && k(r) && se(t) || e.TLiteral(r) && A(r) && de(t) || e.TSymbol(r) && le(t) || e.TBigInt(r) && we(t) || e.TString(r) && ae(t) || e.TSymbol(r) && le(t) || e.TNumber(r) && se(t) || e.TInteger(r) && se(t) || e.TBoolean(r) && de(t) || e.TUint8Array(r) && Pe(t) || e.TDate(r) && he(t) || e.TConstructor(r) && Be(t) || e.TFunction(r) && Se(t) ? o.True : e.TRecord(r) && e.TString(ie(r)) ? t[i.Hint] === "Record" ? o.True : o.False : e.TRecord(r) && e.TNumber(ie(r)) && g(t, 0) ? o.True : o.False;
      }
      function ge(r, t) {
        if (e.TIntersect(t))
          return v(r, t);
        if (e.TUnion(t))
          return N(r, t);
        if (e.TUnknown(t))
          return X();
        if (e.TAny(t))
          return l();
        if (e.TRecord(t))
          return G(r, t);
        if (!e.TObject(t))
          return o.False;
        for (const R of globalThis.Object.keys(t.properties))
          if (!(R in r.properties) || pe(r.properties[R], t.properties[R]) === o.False)
            return o.False;
        return o.True;
      }
      function Ee(r, t) {
        return e.TIntersect(t) ? v(r, t) : e.TUnion(t) ? N(r, t) : e.TUnknown(t) ? X() : e.TAny(t) ? l() : e.TObject(t) && ve(t) ? o.True : e.TPromise(t) ? a(_(r.item, t.item)) : o.False;
      }
      function ie(r) {
        if (i.PatternNumberExact in r.patternProperties)
          return i.Type.Number();
        if (i.PatternStringExact in r.patternProperties)
          return i.Type.String();
        throw Error("TypeExtends: Cannot get record key");
      }
      function fe(r) {
        if (i.PatternNumberExact in r.patternProperties)
          return r.patternProperties[i.PatternNumberExact];
        if (i.PatternStringExact in r.patternProperties)
          return r.patternProperties[i.PatternStringExact];
        throw Error("TypeExtends: Cannot get record value");
      }
      function G(r, t) {
        const R = ie(t), ne = fe(t);
        if (e.TLiteral(r) && W(r) && e.TNumber(R) && a(_(r, ne)) === o.True)
          return o.True;
        if (e.TUint8Array(r) && e.TNumber(R) || e.TString(r) && e.TNumber(R) || e.TArray(r) && e.TNumber(R))
          return _(r, ne);
        if (e.TObject(r)) {
          for (const qe of globalThis.Object.keys(r.properties))
            if (pe(ne, r.properties[qe]) === o.False)
              return o.False;
          return o.True;
        }
        return o.False;
      }
      function Oe(r, t) {
        const R = fe(r);
        return e.TIntersect(t) ? v(r, t) : e.TUnion(t) ? N(r, t) : e.TUnknown(t) ? X() : e.TAny(t) ? l() : e.TObject(t) ? z(r, t) : e.TRecord(t) ? _(R, fe(t)) : o.False;
      }
      function ye(r, t) {
        return e.TLiteral(r) && typeof r.const == "string" || e.TString(r) ? o.True : o.False;
      }
      function Ce(r, t) {
        return e.TIntersect(t) ? v(r, t) : e.TUnion(t) ? N(r, t) : e.TNever(t) ? D() : e.TUnknown(t) ? X() : e.TAny(t) ? l() : e.TObject(t) ? z(r, t) : e.TRecord(t) ? G(r, t) : e.TString(t) ? o.True : o.False;
      }
      function Ue(r, t) {
        return e.TIntersect(t) ? v(r, t) : e.TUnion(t) ? N(r, t) : e.TNever(t) ? D() : e.TUnknown(t) ? X() : e.TAny(t) ? l() : e.TObject(t) ? z(r, t) : e.TRecord(t) ? G(r, t) : e.TSymbol(t) ? o.True : o.False;
      }
      function Re(r, t) {
        return e.TUnknown(r) ? o.False : e.TAny(r) ? o.Union : e.TNever(r) ? o.True : o.False;
      }
      function Le(r, t) {
        return e.TArray(t) && r.items !== void 0 && r.items.every((R) => _(R, t.items) === o.True);
      }
      function V(r, t) {
        return e.TIntersect(t) ? v(r, t) : e.TUnion(t) ? N(r, t) : e.TUnknown(t) ? X() : e.TAny(t) ? l() : e.TObject(t) && re(t) || e.TArray(t) && Le(r, t) ? o.True : !e.TTuple(t) || r.items === void 0 && t.items !== void 0 || r.items !== void 0 && t.items === void 0 ? o.False : r.items === void 0 && t.items === void 0 || r.items.every((R, ne) => _(R, t.items[ne]) === o.True) ? o.True : o.False;
      }
      function n(r, t) {
        return e.TIntersect(t) ? v(r, t) : e.TUnion(t) ? N(r, t) : e.TUnknown(t) ? X() : e.TAny(t) ? l() : e.TObject(t) ? z(r, t) : e.TRecord(t) ? G(r, t) : e.TUint8Array(t) ? o.True : o.False;
      }
      function j(r, t) {
        return e.TIntersect(t) ? v(r, t) : e.TUnion(t) ? N(r, t) : e.TNever(t) ? D() : e.TUnknown(t) ? X() : e.TAny(t) ? l() : e.TObject(t) ? z(r, t) : e.TRecord(t) ? G(r, t) : e.TVoid(t) ? Fe(r) : e.TUndefined(t) ? o.True : o.False;
      }
      function N(r, t) {
        return t.anyOf.some((R) => _(r, R) === o.True) ? o.True : o.False;
      }
      function je(r, t) {
        return r.anyOf.every((R) => _(R, t) === o.True) ? o.True : o.False;
      }
      function X(r, t) {
        return o.True;
      }
      function _e(r, t) {
        return e.TIntersect(t) ? v(r, t) : e.TUnion(t) ? N(r, t) : e.TAny(t) ? l() : e.TString(t) ? ye(r) : e.TNumber(t) ? ce(r) : e.TInteger(t) ? U(r) : e.TBoolean(t) ? m(r) : e.TArray(t) ? f(r) : e.TTuple(t) ? Re(r) : e.TObject(t) ? z(r, t) : e.TUnknown(t) ? o.True : o.False;
      }
      function Fe(r, t) {
        return e.TUndefined(r) || e.TUndefined(r) ? o.True : o.False;
      }
      function He(r, t) {
        return e.TIntersect(t) ? v(r, t) : e.TUnion(t) ? N(r, t) : e.TUnknown(t) ? X() : e.TAny(t) ? l() : e.TObject(t) ? z(r, t) : e.TVoid(t) ? o.True : o.False;
      }
      function _(r, t) {
        if (e.TTemplateLiteral(r))
          return _(x.Resolve(r), t);
        if (e.TTemplateLiteral(t))
          return _(r, x.Resolve(t));
        if (e.TAny(r))
          return y(r, t);
        if (e.TArray(r))
          return p(r, t);
        if (e.TBigInt(r))
          return T(r, t);
        if (e.TBoolean(r))
          return B(r, t);
        if (e.TConstructor(r))
          return C(r, t);
        if (e.TDate(r))
          return I(r, t);
        if (e.TFunction(r))
          return L(r, t);
        if (e.TInteger(r))
          return S(r, t);
        if (e.TIntersect(r))
          return $(r, t);
        if (e.TLiteral(r))
          return K(r, t);
        if (e.TNever(r))
          return Te();
        if (e.TNull(r))
          return me(r, t);
        if (e.TNumber(r))
          return be(r, t);
        if (e.TObject(r))
          return ge(r, t);
        if (e.TRecord(r))
          return Oe(r, t);
        if (e.TString(r))
          return Ce(r, t);
        if (e.TSymbol(r))
          return Ue(r, t);
        if (e.TTuple(r))
          return V(r, t);
        if (e.TPromise(r))
          return Ee(r, t);
        if (e.TUint8Array(r))
          return n(r, t);
        if (e.TUndefined(r))
          return j(r, t);
        if (e.TUnion(r))
          return je(r, t);
        if (e.TUnknown(r))
          return _e(r, t);
        if (e.TVoid(r))
          return He(r, t);
        throw Error(`TypeExtends: Unknown left type operand '${r[i.Kind]}'`);
      }
      function De(r, t) {
        return _(r, t);
      }
      w.Extends = De;
    })(s = i.TypeExtends || (i.TypeExtends = {}));
    var u;
    (function(w) {
      function a(m) {
        return typeof m == "object" && m !== null;
      }
      function l(m) {
        return globalThis.Array.isArray(m);
      }
      function y(m) {
        return m.map((B) => p(B));
      }
      function f(m) {
        const B = globalThis.Object.getOwnPropertyNames(m).reduce((I, L) => ({ ...I, [L]: p(m[L]) }), {}), C = globalThis.Object.getOwnPropertySymbols(m).reduce((I, L) => ({ ...I, [L]: p(m[L]) }), {});
        return { ...B, ...C };
      }
      function p(m) {
        return l(m) ? y(m) : a(m) ? f(m) : m;
      }
      function T(m, B) {
        return { ...p(m), ...B };
      }
      w.Clone = T;
    })(u = i.TypeClone || (i.TypeClone = {}));
    var b;
    (function(w) {
      function a(T, m) {
        return i.Type.Intersect(T.allOf.map((B) => f(B, m)), { ...T });
      }
      function l(T, m) {
        return i.Type.Union(T.anyOf.map((B) => f(B, m)), { ...T });
      }
      function y(T, m) {
        return m(T);
      }
      function f(T, m) {
        return T[i.Kind] === "Intersect" ? a(T, m) : T[i.Kind] === "Union" ? l(T, m) : T[i.Kind] === "Object" ? y(T, m) : T;
      }
      function p(T, m, B) {
        return { ...f(u.Clone(T, {}), m), ...B };
      }
      w.Map = p;
    })(b = i.ObjectMap || (i.ObjectMap = {}));
    var h;
    (function(w) {
      function a(m) {
        return e.TIntersect(m) || e.TUnion(m) || e.TObject(m) && globalThis.Object.getOwnPropertyNames(m.properties).length > 0;
      }
      function l(m) {
        return [...m.allOf.filter((B) => a(B)).reduce((B, C) => p(C).map((I) => B.add(I))[0], /* @__PURE__ */ new Set())];
      }
      function y(m) {
        const B = m.anyOf.filter((C) => a(C)).map((C) => p(C));
        return [...B.reduce((C, I) => I.map((L) => B.every((U) => U.includes(L)) ? C.add(L) : C)[0], /* @__PURE__ */ new Set())];
      }
      function f(m) {
        return globalThis.Object.keys(m.properties);
      }
      function p(m) {
        return e.TIntersect(m) ? l(m) : e.TUnion(m) ? y(m) : e.TObject(m) ? f(m) : [];
      }
      function T(m) {
        return p(m);
      }
      w.Resolve = T;
    })(h = i.KeyResolver || (i.KeyResolver = {}));
    var Q;
    (function(w) {
      function a(f) {
        return f.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
      }
      function l(f, p) {
        if (e.TTemplateLiteral(f))
          return f.pattern.slice(1, f.pattern.length - 1);
        if (e.TUnion(f))
          return `(${f.anyOf.map((m) => l(m, p)).join("|")})`;
        if (e.TNumber(f))
          return `${p}${i.PatternNumber}`;
        if (e.TInteger(f))
          return `${p}${i.PatternNumber}`;
        if (e.TBigInt(f))
          return `${p}${i.PatternNumber}`;
        if (e.TString(f))
          return `${p}${i.PatternString}`;
        if (e.TLiteral(f))
          return `${p}${a(f.const.toString())}`;
        if (e.TBoolean(f))
          return `${p}${i.PatternBoolean}`;
        throw e.TNever(f) ? Error("TemplateLiteralPattern: TemplateLiteral cannot operate on types of TNever") : Error(`TemplateLiteralPattern: Unexpected Kind '${f[i.Kind]}'`);
      }
      function y(f) {
        return `^${f.map((p) => l(p, "")).join("")}$`;
      }
      w.Create = y;
    })(Q = i.TemplateLiteralPattern || (i.TemplateLiteralPattern = {}));
    var x;
    (function(w) {
      function a(l) {
        const y = te.ParseExact(l.pattern);
        if (!Y.Check(y))
          return i.Type.String();
        const f = [...J.Generate(y)].map((p) => i.Type.Literal(p));
        return i.Type.Union(f);
      }
      w.Resolve = a;
    })(x = i.TemplateLiteralResolver || (i.TemplateLiteralResolver = {}));
    class ee extends Error {
      constructor(a) {
        super(a);
      }
    }
    i.TemplateLiteralParserError = ee;
    var te;
    (function(w) {
      function a(S, v, $) {
        return S[v] === $ && S.charCodeAt(v - 1) !== 92;
      }
      function l(S, v) {
        return a(S, v, "(");
      }
      function y(S, v) {
        return a(S, v, ")");
      }
      function f(S, v) {
        return a(S, v, "|");
      }
      function p(S) {
        if (!(l(S, 0) && y(S, S.length - 1)))
          return !1;
        let v = 0;
        for (let $ = 0; $ < S.length; $++)
          if (l(S, $) && (v += 1), y(S, $) && (v -= 1), v === 0 && $ !== S.length - 1)
            return !1;
        return !0;
      }
      function T(S) {
        return S.slice(1, S.length - 1);
      }
      function m(S) {
        let v = 0;
        for (let $ = 0; $ < S.length; $++)
          if (l(S, $) && (v += 1), y(S, $) && (v -= 1), f(S, $) && v === 0)
            return !0;
        return !1;
      }
      function B(S) {
        for (let v = 0; v < S.length; v++)
          if (l(S, v))
            return !0;
        return !1;
      }
      function C(S) {
        let [v, $] = [0, 0];
        const W = [];
        for (let A = 0; A < S.length; A++)
          if (l(S, A) && (v += 1), y(S, A) && (v -= 1), f(S, A) && v === 0) {
            const K = S.slice($, A);
            K.length > 0 && W.push(L(K)), $ = A + 1;
          }
        const k = S.slice($);
        return k.length > 0 && W.push(L(k)), W.length === 0 ? { type: "const", const: "" } : W.length === 1 ? W[0] : { type: "or", expr: W };
      }
      function I(S) {
        function v(k, A) {
          if (!l(k, A))
            throw new ee("TemplateLiteralParser: Index must point to open parens");
          let K = 0;
          for (let D = A; D < k.length; D++)
            if (l(k, D) && (K += 1), y(k, D) && (K -= 1), K === 0)
              return [A, D];
          throw new ee("TemplateLiteralParser: Unclosed group parens in expression");
        }
        function $(k, A) {
          for (let K = A; K < k.length; K++)
            if (l(k, K))
              return [A, K];
          return [A, k.length];
        }
        const W = [];
        for (let k = 0; k < S.length; k++)
          if (l(S, k)) {
            const [A, K] = v(S, k), D = S.slice(A, K + 1);
            W.push(L(D)), k = K;
          } else {
            const [A, K] = $(S, k), D = S.slice(A, K);
            D.length > 0 && W.push(L(D)), k = K - 1;
          }
        return W.length === 0 ? { type: "const", const: "" } : W.length === 1 ? W[0] : { type: "and", expr: W };
      }
      function L(S) {
        return p(S) ? L(T(S)) : m(S) ? C(S) : B(S) ? I(S) : { type: "const", const: S };
      }
      w.Parse = L;
      function U(S) {
        return L(S.slice(1, S.length - 1));
      }
      w.ParseExact = U;
    })(te = i.TemplateLiteralParser || (i.TemplateLiteralParser = {}));
    var Y;
    (function(w) {
      function a(p) {
        return p.type === "or" && p.expr.length === 2 && p.expr[0].type === "const" && p.expr[0].const === "0" && p.expr[1].type === "const" && p.expr[1].const === "[1-9][0-9]*";
      }
      function l(p) {
        return p.type === "or" && p.expr.length === 2 && p.expr[0].type === "const" && p.expr[0].const === "true" && p.expr[1].type === "const" && p.expr[1].const === "false";
      }
      function y(p) {
        return p.type === "const" && p.const === ".*";
      }
      function f(p) {
        if (l(p))
          return !0;
        if (a(p) || y(p))
          return !1;
        if (p.type === "and")
          return p.expr.every((T) => f(T));
        if (p.type === "or")
          return p.expr.every((T) => f(T));
        if (p.type === "const")
          return !0;
        throw Error("TemplateLiteralFinite: Unknown expression type");
      }
      w.Check = f;
    })(Y = i.TemplateLiteralFinite || (i.TemplateLiteralFinite = {}));
    var J;
    (function(w) {
      function* a(T) {
        if (T.length === 1)
          return yield* T[0];
        for (const m of T[0])
          for (const B of a(T.slice(1)))
            yield `${m}${B}`;
      }
      function* l(T) {
        return yield* a(T.expr.map((m) => [...p(m)]));
      }
      function* y(T) {
        for (const m of T.expr)
          yield* p(m);
      }
      function* f(T) {
        return yield T.const;
      }
      function* p(T) {
        if (T.type === "and")
          return yield* l(T);
        if (T.type === "or")
          return yield* y(T);
        if (T.type === "const")
          return yield* f(T);
        throw Error("TemplateLiteralGenerator: Unknown expression");
      }
      w.Generate = p;
    })(J = i.TemplateLiteralGenerator || (i.TemplateLiteralGenerator = {}));
    let Z = 0;
    class oe {
      /** `[Utility]` Creates a schema without `static` and `params` types */
      Create(a) {
        return a;
      }
      /** `[Standard]` Omits compositing symbols from this schema */
      Strict(a) {
        return JSON.parse(JSON.stringify(a));
      }
    }
    i.TypeBuilder = oe;
    class ue extends oe {
      // ------------------------------------------------------------------------
      // Modifiers
      // ------------------------------------------------------------------------
      /** `[Modifier]` Creates a Optional property */
      Optional(a) {
        return { [i.Modifier]: "Optional", ...u.Clone(a, {}) };
      }
      /** `[Modifier]` Creates a ReadonlyOptional property */
      ReadonlyOptional(a) {
        return { [i.Modifier]: "ReadonlyOptional", ...u.Clone(a, {}) };
      }
      /** `[Modifier]` Creates a Readonly object or property */
      Readonly(a) {
        return { [i.Modifier]: "Readonly", ...a };
      }
      // ------------------------------------------------------------------------
      // Types
      // ------------------------------------------------------------------------
      /** `[Standard]` Creates an Any type */
      Any(a = {}) {
        return this.Create({ ...a, [i.Kind]: "Any" });
      }
      /** `[Standard]` Creates an Array type */
      Array(a, l = {}) {
        return this.Create({ ...l, [i.Kind]: "Array", type: "array", items: u.Clone(a, {}) });
      }
      /** `[Standard]` Creates a Boolean type */
      Boolean(a = {}) {
        return this.Create({ ...a, [i.Kind]: "Boolean", type: "boolean" });
      }
      /** `[Standard]` Creates a Composite object type. */
      Composite(a, l) {
        const y = (B, C) => B.every((I) => !(C in I.properties) || f(I.properties[C])), f = (B) => e.TOptional(B) || e.TReadonlyOptional(B), [p, T] = [/* @__PURE__ */ new Set(), /* @__PURE__ */ new Set()];
        for (const B of a)
          for (const C of globalThis.Object.getOwnPropertyNames(B.properties))
            y(a, C) && T.add(C);
        for (const B of a)
          for (const C of globalThis.Object.getOwnPropertyNames(B.properties))
            T.has(C) || p.add(C);
        const m = {};
        for (const B of a)
          for (const [C, I] of Object.entries(B.properties)) {
            const L = u.Clone(I, {});
            if (T.has(C) || delete L[i.Modifier], C in m) {
              const U = s.Extends(m[C], L) !== o.False, S = s.Extends(L, m[C]) !== o.False;
              !U && !S && (m[C] = i.Type.Never()), !U && S && (m[C] = L);
            } else
              m[C] = L;
          }
        return p.size > 0 ? this.Create({ ...l, [i.Kind]: "Object", [i.Hint]: "Composite", type: "object", properties: m, required: [...p] }) : this.Create({ ...l, [i.Kind]: "Object", [i.Hint]: "Composite", type: "object", properties: m });
      }
      /** `[Standard]` Creates a Enum type */
      Enum(a, l = {}) {
        const f = globalThis.Object.keys(a).filter((p) => isNaN(p)).map((p) => a[p]).map((p) => typeof p == "string" ? { [i.Kind]: "Literal", type: "string", const: p } : { [i.Kind]: "Literal", type: "number", const: p });
        return this.Create({ ...l, [i.Kind]: "Union", anyOf: f });
      }
      /** `[Standard]` A conditional type expression that will return the true type if the left type extends the right */
      Extends(a, l, y, f, p = {}) {
        switch (s.Extends(a, l)) {
          case o.Union:
            return this.Union([u.Clone(y, p), u.Clone(f, p)]);
          case o.True:
            return u.Clone(y, p);
          case o.False:
            return u.Clone(f, p);
        }
      }
      /** `[Standard]` Excludes from the left type any type that is not assignable to the right */
      Exclude(a, l, y = {}) {
        if (e.TTemplateLiteral(a))
          return this.Exclude(x.Resolve(a), l, y);
        if (e.TTemplateLiteral(l))
          return this.Exclude(a, x.Resolve(l), y);
        if (e.TUnion(a)) {
          const f = a.anyOf.filter((p) => s.Extends(p, l) === o.False);
          return f.length === 1 ? u.Clone(f[0], y) : this.Union(f, y);
        } else
          return s.Extends(a, l) !== o.False ? this.Never(y) : u.Clone(a, y);
      }
      /** `[Standard]` Extracts from the left type any type that is assignable to the right */
      Extract(a, l, y = {}) {
        if (e.TTemplateLiteral(a))
          return this.Extract(x.Resolve(a), l, y);
        if (e.TTemplateLiteral(l))
          return this.Extract(a, x.Resolve(l), y);
        if (e.TUnion(a)) {
          const f = a.anyOf.filter((p) => s.Extends(p, l) !== o.False);
          return f.length === 1 ? u.Clone(f[0], y) : this.Union(f, y);
        } else
          return s.Extends(a, l) !== o.False ? u.Clone(a, y) : this.Never(y);
      }
      /** `[Standard]` Creates an Integer type */
      Integer(a = {}) {
        return this.Create({ ...a, [i.Kind]: "Integer", type: "integer" });
      }
      Intersect(a, l = {}) {
        if (a.length === 0)
          return i.Type.Never();
        if (a.length === 1)
          return u.Clone(a[0], l);
        const y = a.every((T) => e.TObject(T)), f = a.map((T) => u.Clone(T, {})), p = e.TSchema(l.unevaluatedProperties) ? { unevaluatedProperties: u.Clone(l.unevaluatedProperties, {}) } : {};
        return l.unevaluatedProperties === !1 || e.TSchema(l.unevaluatedProperties) || y ? this.Create({ ...l, ...p, [i.Kind]: "Intersect", type: "object", allOf: f }) : this.Create({ ...l, ...p, [i.Kind]: "Intersect", allOf: f });
      }
      /** `[Standard]` Creates a KeyOf type */
      KeyOf(a, l = {}) {
        if (e.TRecord(a)) {
          const y = Object.getOwnPropertyNames(a.patternProperties)[0];
          if (y === i.PatternNumberExact)
            return this.Number(l);
          if (y === i.PatternStringExact)
            return this.String(l);
          throw Error("StandardTypeBuilder: Unable to resolve key type from Record key pattern");
        } else {
          const y = h.Resolve(a);
          if (y.length === 0)
            return this.Never(l);
          const f = y.map((p) => this.Literal(p));
          return this.Union(f, l);
        }
      }
      /** `[Standard]` Creates a Literal type */
      Literal(a, l = {}) {
        return this.Create({ ...l, [i.Kind]: "Literal", const: a, type: typeof a });
      }
      /** `[Standard]` Creates a Never type */
      Never(a = {}) {
        return this.Create({ ...a, [i.Kind]: "Never", not: {} });
      }
      /** `[Standard]` Creates a Not type. The first argument is the disallowed type, the second is the allowed. */
      Not(a, l, y) {
        return this.Create({ ...y, [i.Kind]: "Not", allOf: [{ not: u.Clone(a, {}) }, u.Clone(l, {})] });
      }
      /** `[Standard]` Creates a Null type */
      Null(a = {}) {
        return this.Create({ ...a, [i.Kind]: "Null", type: "null" });
      }
      /** `[Standard]` Creates a Number type */
      Number(a = {}) {
        return this.Create({ ...a, [i.Kind]: "Number", type: "number" });
      }
      /** `[Standard]` Creates an Object type */
      Object(a, l = {}) {
        const y = globalThis.Object.getOwnPropertyNames(a), f = y.filter((B) => e.TOptional(a[B]) || e.TReadonlyOptional(a[B])), p = y.filter((B) => !f.includes(B)), T = e.TSchema(l.additionalProperties) ? { additionalProperties: u.Clone(l.additionalProperties, {}) } : {}, m = y.reduce((B, C) => ({ ...B, [C]: u.Clone(a[C], {}) }), {});
        return p.length > 0 ? this.Create({ ...l, ...T, [i.Kind]: "Object", type: "object", properties: m, required: p }) : this.Create({ ...l, ...T, [i.Kind]: "Object", type: "object", properties: m });
      }
      Omit(a, l, y = {}) {
        const f = e.TUnionLiteral(l) ? l.anyOf.map((p) => p.const) : e.TLiteral(l) ? [l.const] : e.TNever(l) ? [] : l;
        return b.Map(u.Clone(a, {}), (p) => {
          p.required && (p.required = p.required.filter((T) => !f.includes(T)), p.required.length === 0 && delete p.required);
          for (const T of globalThis.Object.keys(p.properties))
            f.includes(T) && delete p.properties[T];
          return this.Create(p);
        }, y);
      }
      /** `[Standard]` Creates a mapped type where all properties are Optional */
      Partial(a, l = {}) {
        function y(f) {
          switch (f[i.Modifier]) {
            case "ReadonlyOptional":
              f[i.Modifier] = "ReadonlyOptional";
              break;
            case "Readonly":
              f[i.Modifier] = "ReadonlyOptional";
              break;
            case "Optional":
              f[i.Modifier] = "Optional";
              break;
            default:
              f[i.Modifier] = "Optional";
              break;
          }
        }
        return b.Map(u.Clone(a, {}), (f) => (delete f.required, globalThis.Object.keys(f.properties).forEach((p) => y(f.properties[p])), f), l);
      }
      Pick(a, l, y = {}) {
        const f = e.TUnionLiteral(l) ? l.anyOf.map((p) => p.const) : e.TLiteral(l) ? [l.const] : e.TNever(l) ? [] : l;
        return b.Map(u.Clone(a, {}), (p) => {
          p.required && (p.required = p.required.filter((T) => f.includes(T)), p.required.length === 0 && delete p.required);
          for (const T of globalThis.Object.keys(p.properties))
            f.includes(T) || delete p.properties[T];
          return this.Create(p);
        }, y);
      }
      /** `[Standard]` Creates a Record type */
      Record(a, l, y = {}) {
        if (e.TTemplateLiteral(a)) {
          const f = te.ParseExact(a.pattern);
          return Y.Check(f) ? this.Object([...J.Generate(f)].reduce((p, T) => ({ ...p, [T]: u.Clone(l, {}) }), {}), y) : this.Create({ ...y, [i.Kind]: "Record", type: "object", patternProperties: { [a.pattern]: u.Clone(l, {}) }, additionalProperties: !1 });
        } else if (e.TUnionLiteral(a))
          if (a.anyOf.every((f) => e.TLiteral(f) && (typeof f.const == "string" || typeof f.const == "number"))) {
            const f = a.anyOf.reduce((p, T) => ({ ...p, [T.const]: u.Clone(l, {}) }), {});
            return this.Object(f, { ...y, [i.Hint]: "Record" });
          } else
            throw Error("TypeBuilder: Record key can only be derived from union literal of number or string");
        else if (e.TLiteral(a)) {
          if (typeof a.const == "string" || typeof a.const == "number")
            return this.Object({ [a.const]: u.Clone(l, {}) }, y);
          throw Error("TypeBuilder: Record key can only be derived from literals of number or string");
        } else if (e.TInteger(a) || e.TNumber(a)) {
          const f = i.PatternNumberExact;
          return this.Create({ ...y, [i.Kind]: "Record", type: "object", patternProperties: { [f]: u.Clone(l, {}) }, additionalProperties: !1 });
        } else if (e.TString(a)) {
          const f = a.pattern === void 0 ? i.PatternStringExact : a.pattern;
          return this.Create({ ...y, [i.Kind]: "Record", type: "object", patternProperties: { [f]: u.Clone(l, {}) }, additionalProperties: !1 });
        } else
          throw Error("StandardTypeBuilder: Invalid Record Key");
      }
      /** `[Standard]` Creates a Recursive type */
      Recursive(a, l = {}) {
        l.$id === void 0 && (l.$id = `T${Z++}`);
        const y = a({ [i.Kind]: "This", $ref: `${l.$id}` });
        return y.$id = l.$id, this.Create({ ...l, [i.Hint]: "Recursive", ...y });
      }
      /** `[Standard]` Creates a Ref type. The referenced type must contain a $id */
      Ref(a, l = {}) {
        if (a.$id === void 0)
          throw Error("StandardTypeBuilder.Ref: Target type must specify an $id");
        return this.Create({ ...l, [i.Kind]: "Ref", $ref: a.$id });
      }
      /** `[Standard]` Creates a mapped type where all properties are Required */
      Required(a, l = {}) {
        function y(f) {
          switch (f[i.Modifier]) {
            case "ReadonlyOptional":
              f[i.Modifier] = "Readonly";
              break;
            case "Readonly":
              f[i.Modifier] = "Readonly";
              break;
            case "Optional":
              delete f[i.Modifier];
              break;
            default:
              delete f[i.Modifier];
              break;
          }
        }
        return b.Map(u.Clone(a, {}), (f) => (f.required = globalThis.Object.keys(f.properties), globalThis.Object.keys(f.properties).forEach((p) => y(f.properties[p])), f), l);
      }
      /** `[Standard]` Creates a String type */
      String(a = {}) {
        return this.Create({ ...a, [i.Kind]: "String", type: "string" });
      }
      /** `[Standard]` Creates a template literal type */
      TemplateLiteral(a, l = {}) {
        const y = Q.Create(a);
        return this.Create({ ...l, [i.Kind]: "TemplateLiteral", type: "string", pattern: y });
      }
      /** `[Standard]` Creates a Tuple type */
      Tuple(a, l = {}) {
        const [y, f, p] = [!1, a.length, a.length], T = a.map((B) => u.Clone(B, {})), m = a.length > 0 ? { ...l, [i.Kind]: "Tuple", type: "array", items: T, additionalItems: y, minItems: f, maxItems: p } : { ...l, [i.Kind]: "Tuple", type: "array", minItems: f, maxItems: p };
        return this.Create(m);
      }
      Union(a, l = {}) {
        if (e.TTemplateLiteral(a))
          return x.Resolve(a);
        {
          const y = a;
          if (y.length === 0)
            return this.Never(l);
          if (y.length === 1)
            return this.Create(u.Clone(y[0], l));
          const f = y.map((p) => u.Clone(p, {}));
          return this.Create({ ...l, [i.Kind]: "Union", anyOf: f });
        }
      }
      /** `[Standard]` Creates an Unknown type */
      Unknown(a = {}) {
        return this.Create({ ...a, [i.Kind]: "Unknown" });
      }
      /** `[Standard]` Creates a Unsafe type that infers for the generic argument */
      Unsafe(a = {}) {
        return this.Create({ ...a, [i.Kind]: a[i.Kind] || "Unsafe" });
      }
    }
    i.StandardTypeBuilder = ue;
    class ke extends ue {
      /** `[Extended]` Creates a BigInt type */
      BigInt(a = {}) {
        return this.Create({ ...a, [i.Kind]: "BigInt", type: "null", typeOf: "BigInt" });
      }
      /** `[Extended]` Extracts the ConstructorParameters from the given Constructor type */
      ConstructorParameters(a, l = {}) {
        return this.Tuple([...a.parameters], { ...l });
      }
      Constructor(a, l, y = {}) {
        const f = u.Clone(l, {});
        if (e.TTuple(a)) {
          const p = a.items === void 0 ? [] : a.items.map((T) => u.Clone(T, {}));
          return this.Create({ ...y, [i.Kind]: "Constructor", type: "object", instanceOf: "Constructor", parameters: p, returns: f });
        } else if (globalThis.Array.isArray(a)) {
          const p = a.map((T) => u.Clone(T, {}));
          return this.Create({ ...y, [i.Kind]: "Constructor", type: "object", instanceOf: "Constructor", parameters: p, returns: f });
        } else
          throw new Error("ExtendedTypeBuilder.Constructor: Invalid parameters");
      }
      /** `[Extended]` Creates a Date type */
      Date(a = {}) {
        return this.Create({ ...a, [i.Kind]: "Date", type: "object", instanceOf: "Date" });
      }
      Function(a, l, y = {}) {
        const f = u.Clone(l, {});
        if (e.TTuple(a)) {
          const p = a.items === void 0 ? [] : a.items.map((T) => u.Clone(T, {}));
          return this.Create({ ...y, [i.Kind]: "Function", type: "object", instanceOf: "Function", parameters: p, returns: f });
        } else if (globalThis.Array.isArray(a)) {
          const p = a.map((T) => u.Clone(T, {}));
          return this.Create({ ...y, [i.Kind]: "Function", type: "object", instanceOf: "Function", parameters: p, returns: f });
        } else
          throw new Error("ExtendedTypeBuilder.Function: Invalid parameters");
      }
      /** `[Extended]` Extracts the InstanceType from the given Constructor */
      InstanceType(a, l = {}) {
        return u.Clone(a.returns, l);
      }
      /** `[Extended]` Extracts the Parameters from the given Function type */
      Parameters(a, l = {}) {
        return this.Tuple(a.parameters, { ...l });
      }
      /** `[Extended]` Creates a Promise type */
      Promise(a, l = {}) {
        return this.Create({ ...l, [i.Kind]: "Promise", type: "object", instanceOf: "Promise", item: u.Clone(a, {}) });
      }
      /** `[Extended]` Creates a regular expression type */
      RegEx(a, l = {}) {
        return this.Create({ ...l, [i.Kind]: "String", type: "string", pattern: a.source });
      }
      /** `[Extended]` Extracts the ReturnType from the given Function */
      ReturnType(a, l = {}) {
        return u.Clone(a.returns, l);
      }
      /** `[Extended]` Creates a Symbol type */
      Symbol(a) {
        return this.Create({ ...a, [i.Kind]: "Symbol", type: "null", typeOf: "Symbol" });
      }
      /** `[Extended]` Creates a Undefined type */
      Undefined(a = {}) {
        return this.Create({ ...a, [i.Kind]: "Undefined", type: "null", typeOf: "Undefined" });
      }
      /** `[Extended]` Creates a Uint8Array type */
      Uint8Array(a = {}) {
        return this.Create({ ...a, [i.Kind]: "Uint8Array", type: "object", instanceOf: "Uint8Array" });
      }
      /** `[Extended]` Creates a Void type */
      Void(a = {}) {
        return this.Create({ ...a, [i.Kind]: "Void", type: "null", typeOf: "Void" });
      }
    }
    i.ExtendedTypeBuilder = ke, i.StandardType = new ue(), i.Type = new ke();
  }(Ne)), Ne;
}
var F = Xe(), H = /* @__PURE__ */ ((i) => (i.AUTHORIZED = "Authorized", i.REJECTED = "Rejected", i))(H || {});
const Ye = F.Type.Enum(H);
F.Type.Object({
  paymentMethod: F.Type.Object({
    type: F.Type.String(),
    accHolder: F.Type.Optional(F.Type.String()),
    birthdate: F.Type.Optional(F.Type.String()),
    accountNumber: F.Type.Optional(F.Type.String()),
    routingNumber: F.Type.Optional(F.Type.String()),
    panHash: F.Type.Optional(F.Type.String()),
    uniqueId: F.Type.Optional(F.Type.String()),
    doRedirect: F.Type.Optional(F.Type.String()),
    returnUrl: F.Type.Optional(F.Type.String())
  }),
  paymentOutcome: Ye,
  lang: F.Type.Optional(F.Type.String()),
  path: F.Type.Optional(F.Type.String())
});
class Je {
  constructor(d) {
    P(this, "componentHasSubmit", !0);
    this.baseOptions = d;
  }
  build(d) {
    return new Ve(
      this.baseOptions,
      d
    );
  }
}
class Ve extends M {
  constructor(c, e) {
    super(
      q.invoice,
      c,
      e
    );
    P(this, "showPayButton");
    this.showPayButton = (e == null ? void 0 : e.showPayButton) ?? !1;
  }
  mount(c) {
    const e = c.replace(/\|/g, "\\|"), o = document.querySelector(e);
    if (!o) {
      console.error("Container not found:", e);
      return;
    }
    if (o.insertAdjacentHTML("beforeend", this._getTemplate()), this.showPayButton) {
      const s = document.querySelector("#invoiceForm-paymentButton");
      s && s.addEventListener(
        "click",
        (u) => {
          u.preventDefault(), this.submit();
        }
      );
    }
  }
  async submit() {
    this.sdk.init({
      environment: this.environment
    });
    const c = window.location.pathname.split("/")[1], o = new URL(window.location.href).origin;
    try {
      const s = {
        paymentMethod: {
          type: "INVOICE"
        },
        paymentOutcome: H.AUTHORIZED,
        lang: c ?? "de",
        path: o
      }, u = await fetch(
        this.processorUrl + "/directPayment",
        {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            "X-Session-Id": this.sessionId
          },
          body: JSON.stringify(
            s
          )
        }
      );
      if (!u.ok) {
        const h = await u.text();
        throw console.error("HTTP error response:", h), new Error(`HTTP error! status: ${u.status}`);
      }
      const b = await u.json();
      b.paymentReference ? this.onComplete && this.onComplete({
        isSuccess: !0,
        paymentReference: b.paymentReference
      }) : this.onError("Some error occurred. Please try again.");
    } catch (s) {
      console.error("Invoice payment error:", s), this.onError("Some error occurred. Please try again.");
    }
  }
  _getTemplate() {
    const c = document.documentElement.lang || "en", e = c.startsWith("de") ? "Bezahlen Sie bequem per Rechnung und überweisen Sie den Betrag innerhalb der angegebenen Frist." : "Pay easily with Invoice and transfer the shopping amount within the specified date.";
    return this.showPayButton ? `<div class="${O.wrapper}">
        <p> ${e} </p>
        <button class="${E.button} ${E.fullWidth} ${O.submitButton}" id="invoiceForm-paymentButton">
          ${c.startsWith("de") ? "Bezahlen" : "Pay"}
        </button>
      </div>` : "";
  }
}
class Ze {
  constructor(d) {
    P(this, "componentHasSubmit", !0);
    this.baseOptions = d;
  }
  build(d) {
    return new Ge(this.baseOptions, d);
  }
}
class Ge extends M {
  constructor(c, e) {
    super(q.prepayment, c, e);
    P(this, "showPayButton");
    this.showPayButton = (e == null ? void 0 : e.showPayButton) ?? !1;
  }
  mount(c) {
    const e = c.replace(/\|/g, "\\|"), o = document.querySelector(e);
    if (!o) {
      console.error("Container not found:", e);
      return;
    }
    if (o.insertAdjacentHTML("beforeend", this._getTemplate()), this.showPayButton) {
      const s = document.querySelector("#purchaseOrderForm-paymentButton");
      s && s.addEventListener(
        "click",
        (u) => {
          u.preventDefault(), this.submit();
        }
      );
    }
  }
  async submit() {
    this.sdk.init({ environment: this.environment });
    const c = window.location.pathname.split("/")[1], o = new URL(window.location.href).origin;
    try {
      const s = {
        paymentMethod: {
          type: "PREPAYMENT"
        },
        paymentOutcome: H.AUTHORIZED,
        lang: c ?? "de",
        path: o
      }, b = await (await fetch(this.processorUrl + "/directPayment", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "X-Session-Id": this.sessionId
        },
        body: JSON.stringify(s)
      })).json();
      b.paymentReference ? this.onComplete && this.onComplete({
        isSuccess: !0,
        paymentReference: b.paymentReference
      }) : this.onError("Some error occurred. Please try again.");
    } catch {
      this.onError("Some error occurred. Please try again.");
    }
  }
  _getTemplate() {
    const c = document.documentElement.lang || "en", e = c.startsWith("de") ? "Bezahlen Sie ganz einfach per Vorauskasse und überweisen Sie den Kaufbetrag innerhalb der angegebenen Frist." : "Pay easily with Prepayment and transfer the shopping amount within the specified date.";
    return this.showPayButton ? `<div class="${O.wrapper}">
        <p> ${e} </p>
        <button class="${E.button} ${E.fullWidth} ${O.submitButton}" id="purchaseOrderForm-paymentButton">
          ${c.startsWith("de") ? "Bezahlen" : "Pay"}
        </button>
      </div>` : "";
  }
}
class Qe {
  constructor(d) {
    P(this, "componentHasSubmit", !0);
    this.baseOptions = d;
  }
  build(d) {
    return new et(this.baseOptions, d);
  }
}
class et extends M {
  constructor(c, e) {
    super(q.GuaranteedInvoice, c, e);
    P(this, "showPayButton");
    this.showPayButton = (e == null ? void 0 : e.showPayButton) ?? !1;
  }
  mount(c) {
    const e = "#" + CSS.escape(
      c.substring(1)
    ), o = document.querySelector(
      e
    );
    if (!o) {
      console.error(
        "Container not found:",
        e
      );
      return;
    }
    if (o.insertAdjacentHTML(
      "afterbegin",
      this._getTemplate()
    ), setTimeout(() => {
      var u;
      const s = o.querySelector(
        "label"
      );
      s && ((u = s.textContent) != null && u.toLowerCase().includes("GuaranteedInvoice")) && (s.textContent = "Invoice with payment guarantee");
    }, 100), this.showPayButton) {
      const s = document.querySelector(
        "#GuaranteedInvoiceForm-paymentButton"
      );
      s && s.addEventListener(
        "click",
        (u) => {
          u.preventDefault(), this.submit();
        }
      );
    }
  }
  async submit() {
    this.sdk.init({ environment: this.environment }), window.location.pathname.split("/")[1], new URL(window.location.href).origin;
    try {
      const e = document.getElementById("nn_birthdate"), s = {
        paymentMethod: {
          type: "GUARANTEED_INVOICE",
          birthdate: (e == null ? void 0 : e.value.trim()) ?? ""
        },
        paymentOutcome: H.AUTHORIZED
      }, b = await (await fetch(this.processorUrl + "/directPayment", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "X-Session-Id": this.sessionId
        },
        body: JSON.stringify(s)
      })).json();
      b.paymentReference ? this.onComplete && this.onComplete({
        isSuccess: !0,
        paymentReference: b.paymentReference
      }) : this.onError("Some error occurred. Please try again.");
    } catch {
      this.onError("Some error occurred. Please try again.");
    }
  }
  _getTemplate() {
    return this.showPayButton && `${O.wrapper}${E.button}${E.fullWidth}${O.submitButton}`, `
        <div style="width:100%; display:flex; flex-direction:column;">
          <script type="text/javascript" src="https://cdn.novalnet.de/js/v2/NovalnetUtility.js"><\/script>

            <!-- Birthdate -->
            <div style="display:flex; flex-direction:column; width:100%;">
            <label for="nn_birthdate"
                style="font-size:14px; font-weight:600; color:#333; margin-bottom:6px;"
            >
                Birthdate (DD-MM-YYYY) <span style="color:red;">*</span>
            </label>

            <input
                type="text"
                id="nn_birthdate"
                name="nn_birthdate"
                placeholder="DD-MM-YYYY"
                maxlength="10"
                style="
                padding:12px 14px;
                border:1.5px solid #d4d4d4;
                border-radius:6px;
                font-size:15px;
                transition:all 0.2s ease-in-out;
                "
            />

            <span
                id="nn_birthdate_error"
                style="
                display:none;
                margin-top:4px;
                font-size:12px;
                color:#d70000;
                "
            >Invalid birthdate</span>
            </div> 
        </form>
        </div>
      `;
  }
}
class tt {
  constructor(d) {
    P(this, "componentHasSubmit", !0);
    this.baseOptions = d;
  }
  build(d) {
    return new nt(this.baseOptions, d);
  }
}
class nt extends M {
  constructor(c, e) {
    super(q.GuaranteedSepa, c, e);
    P(this, "showPayButton");
    this.showPayButton = (e == null ? void 0 : e.showPayButton) ?? !1;
  }
  mount(c) {
    const e = "#" + CSS.escape(
      c.substring(1)
    ), o = document.querySelector(
      e
    );
    if (!o) {
      console.error(
        "Container not found:",
        e
      );
      return;
    }
    if (o.insertAdjacentHTML(
      "afterbegin",
      this._getTemplate()
    ), setTimeout(() => {
      var u;
      const s = o.querySelector(
        "label"
      );
      s && ((u = s.textContent) != null && u.toLowerCase().includes("GuaranteedSepa")) && (s.textContent = "Direct Debit SEPA with payment guarantee");
    }, 100), this.showPayButton) {
      const s = document.querySelector(
        "#purchaseOrderForm-paymentButton"
      );
      s && s.addEventListener(
        "click",
        (u) => {
          u.preventDefault(), this.submit();
        }
      );
    }
  }
  async submit() {
    this.sdk.init({ environment: this.environment });
    const c = window.location.pathname.split("/")[1], o = new URL(window.location.href).origin;
    try {
      const s = document.getElementById("nn_account_holder"), u = document.getElementById("nn_guaranteesepa_account_no"), b = document.getElementById("nn_guaranteesepa_bic"), h = document.getElementById("nn_sepa_birthdate"), Q = (s == null ? void 0 : s.value.trim()) ?? "", x = (u == null ? void 0 : u.value.trim()) ?? "", ee = (b == null ? void 0 : b.value.trim()) ?? "", te = (h == null ? void 0 : h.value.trim()) ?? "", Y = {
        paymentMethod: {
          type: "GUARANTEED_DIRECT_DEBIT_SEPA",
          accHolder: Q,
          iban: x,
          bic: ee,
          birthdate: te
        },
        paymentOutcome: H.AUTHORIZED,
        lang: c ?? "de",
        path: o
      }, Z = await (await fetch(this.processorUrl + "/directPayment", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "X-Session-Id": this.sessionId
        },
        body: JSON.stringify(Y)
      })).json();
      Z.paymentReference ? this.onComplete && this.onComplete({
        isSuccess: !0,
        paymentReference: Z.paymentReference
      }) : this.onError("Some error occurred. Please try again.");
    } catch {
      this.onError("Some error occurred. Please try again.");
    }
  }
  _getTemplate() {
    return `
        <div style="width:100%; display:flex; flex-direction:column;">
          <script type="text/javascript" src="https://cdn.novalnet.de/js/v2/NovalnetUtility.js"><\/script>
    
          <form id="nn_guaranteesepa_form"
            style="
              width:100%;
              display:flex;
              flex-direction:column;
              gap:20px;
            "
          >
    
            <!-- Account Holder -->
            <div style="display:flex; flex-direction:column; width:100%;">
              <label for="nn_account_holder"
                style="font-size:14px; font-weight:600; color:#333; margin-bottom:6px;"
              >
                Account Holder <span style="color:red;">*</span>
              </label>
    
              <input
                type="text"
                id="nn_account_holder"
                name="nn_account_holder"
                style="
                  padding:12px 14px;
                  border:1.5px solid #d4d4d4;
                  border-radius:6px;
                  font-size:15px;
                  transition:all 0.2s ease-in-out;
                "
              />
    
              <span
                style="
                  display:none;
                  margin-top:4px;
                  font-size:12px;
                  color:#d70000;
                "
              >Invalid account holder</span>
            </div>
    
            <!-- IBAN -->
            <div style="display:flex; flex-direction:column; width:100%;">
              <label for="nn_guaranteesepa_account_no"
                style="font-size:14px; font-weight:600; color:#333; margin-bottom:6px;"
              >
                IBAN <span style="color:red;">*</span>
              </label>
    
              <input
                type="text"
                id="nn_guaranteesepa_account_no"
                name="nn_guaranteesepa_account_no"
                size="32"
                autocomplete="off"
                onkeypress="return NovalnetUtility.checkIban(event, 'bic_div');"
                onkeyup="return NovalnetUtility.formatIban(event, 'bic_div');"
                onchange="return NovalnetUtility.formatIban(event, 'bic_div');"
                style="
                  padding:12px 14px;
                  border:1.5px solid #d4d4d4;
                  border-radius:6px;
                  font-size:15px;
                  text-transform:uppercase;
                  transition:all 0.2s ease-in-out;
                "
              />
    
              <span
                style="
                  display:none;
                  margin-top:4px;
                  font-size:12px;
                  color:#d70000;
                "
              >Invalid IBAN</span>
            </div>
    
            <!-- BIC FIELD -->
            <div id="bic_div" style="display:none; flex-direction:column; width:100%;">
              <label for="nn_guaranteesepa_bic"
                style="font-size:14px; font-weight:600; color:#333; margin-bottom:6px;"
              >
                BIC <span style="color:red;">*</span>
              </label>
    
              <input
                type="text"
                id="nn_guaranteesepa_bic"
                name="nn_guaranteesepa_bic"
                size="32"
                autocomplete="off"
                onkeypress="return NovalnetUtility.formatBic(event);"
                onchange="return NovalnetUtility.formatBic(event);"
                style="
                  padding:12px 14px;
                  border:1.5px solid #d4d4d4;
                  border-radius:6px;
                  font-size:15px;
                  transition:all 0.2s ease-in-out;
                "
              />
    
              <span
                style="
                  display:none;
                  margin-top:4px;
                  font-size:12px;
                  color:#d70000;
                "
              >Invalid BIC</span>
            </div>

           <!-- Birthdate -->
            <div style="display:flex; flex-direction:column; width:100%;">
            <label for="nn_sepa_birthdate"
                style="font-size:14px; font-weight:600; color:#333; margin-bottom:6px;"
            >
                Birthdate (DD-MM-YYYY) <span style="color:red;">*</span>
            </label>

            <input
                type="text"
                id="nn_sepa_birthdate"
                name="nn_sepa_birthdate"
                placeholder="DD-MM-YYYY"
                maxlength="10"
                style="
                padding:12px 14px;
                border:1.5px solid #d4d4d4;
                border-radius:6px;
                font-size:15px;
                transition:all 0.2s ease-in-out;
                "
            />

            <span
                id="nn_sepa_birthdate_error"
                style="
                display:none;
                margin-top:4px;
                font-size:12px;
                color:#d70000;
                "
            >Invalid birthdate</span>
            </div>


            ${this.showPayButton ? `<button class="${E.button} ${E.fullWidth} ${O.submitButton}" 
             id="purchaseOrderForm-paymentButton">Pay</button>` : ""}
          </form>
        </div>
      `;
  }
}
class rt {
  constructor(d) {
    P(this, "componentHasSubmit", !0);
    this.baseOptions = d;
  }
  build(d) {
    return new it(this.baseOptions, d);
  }
}
class it extends M {
  constructor(c, e) {
    super(q.ideal, c, e);
    P(this, "showPayButton");
    this.showPayButton = (e == null ? void 0 : e.showPayButton) ?? !1;
  }
  mount(c) {
    const e = c.replace(/\|/g, "\\|"), o = document.querySelector(e);
    if (!o) {
      console.error(
        "Container not found:",
        e
      );
      return;
    }
    if (o.insertAdjacentHTML(
      "afterbegin",
      this._getTemplate()
    ), setTimeout(() => {
      document.querySelectorAll("label").forEach((u) => {
        var h;
        const b = (h = u.textContent) == null ? void 0 : h.trim().toLowerCase();
        b != null && b.includes("ideal") && (u.textContent = "iDEAL | Wero");
      });
    }, 300), this.showPayButton) {
      const s = document.querySelector("#purchaseOrderForm-paymentButton");
      s && s.addEventListener(
        "click",
        (u) => {
          u.preventDefault(), this.submit();
        }
      );
    }
  }
  async submit() {
    this.sdk.init({
      environment: this.environment
    });
    const c = window.location.pathname.split("/")[1], o = new URL(window.location.href).origin;
    try {
      const s = {
        paymentMethod: {
          type: "IDEAL"
        },
        paymentOutcome: H.AUTHORIZED,
        lang: c ?? "de",
        path: o
      }, u = await fetch(
        this.processorUrl + "/redirectPayment",
        {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            "X-Session-Id": this.sessionId
          },
          body: JSON.stringify(
            s
          )
        }
      );
      if (!u.ok) {
        const h = await u.text();
        throw console.error(
          "HTTP error response:",
          h
        ), new Error(
          `HTTP error! status: ${u.status}`
        );
      }
      const b = await u.json();
      window.location.href = b.txnSecret;
    } catch (s) {
      console.error(
        "Error details:",
        {
          message: s.message,
          stack: s.stack,
          name: s.name
        }
      ), this.onError(
        "Some error occurred. Please try again."
      );
    }
  }
  _getTemplate() {
    return this.showPayButton ? `
    <div class="${O.wrapper}">

      <p>
        Pay easily with iDEAL | Wero and transfer the shopping amount within the specified date.
      </p>

      <button
        class="${E.button}
        ${E.fullWidth}
        ${O.submitButton}"

        id="purchaseOrderForm-paymentButton"
      >
        Pay Now
      </button>

    </div>
    ` : "";
  }
}
class ot {
  constructor(d) {
    P(this, "componentHasSubmit", !0);
    this.baseOptions = d;
  }
  build(d) {
    return new at(this.baseOptions, d);
  }
}
class at extends M {
  constructor(c, e) {
    super(q.paypal, c, e);
    P(this, "showPayButton");
    this.showPayButton = (e == null ? void 0 : e.showPayButton) ?? !1;
  }
  mount(c) {
    document.querySelector(c).insertAdjacentHTML("afterbegin", this._getTemplate()), this.showPayButton && document.querySelector("#purchaseOrderForm-paymentButton").addEventListener("click", (e) => {
      e.preventDefault(), this.submit();
    });
  }
  async submit() {
    this.sdk.init({ environment: this.environment });
    const c = window.location.pathname.split("/")[1], o = new URL(window.location.href).origin;
    try {
      const s = {
        paymentMethod: {
          type: this.paymentMethod
        },
        paymentOutcome: H.AUTHORIZED,
        lang: c,
        path: o
      }, u = await fetch(this.processorUrl + "/redirectPayment", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "X-Session-Id": this.sessionId
        },
        body: JSON.stringify(s)
      });
      if (!u.ok) {
        const h = await u.text();
        throw console.error("HTTP error response:", h), new Error(`HTTP error! status: ${u.status}`);
      }
      const b = await u.json();
      window.location.href = b.txnSecret;
    } catch (s) {
      console.error("=== PAYMENT SUBMISSION ERROR ===:", s), console.error("Error details:", {
        message: s.message,
        stack: s.stack,
        name: s.name
      }), this.onError("Some error occurred. Please try again.");
    }
  }
  _getTemplate() {
    return this.showPayButton ? `
    <div class="${O.wrapper}">
      <p>Pay easily with Paypal and transfer the shopping amount within the specified date.</p>
      <button class="${E.button} ${E.fullWidth} ${O.submitButton}" id="purchaseOrderForm-paymentButton">Pay Now</button>
    </div>
    ` : "";
  }
}
class st {
  constructor(d) {
    P(this, "componentHasSubmit", !0);
    this.baseOptions = d;
  }
  build(d) {
    return new ut(
      this.baseOptions,
      d
    );
  }
}
class ut extends M {
  constructor(c, e) {
    super(
      q.onlinebanktransfer,
      c,
      e
    );
    P(this, "showPayButton");
    P(this, "isSubmitting", !1);
    this.showPayButton = (e == null ? void 0 : e.showPayButton) ?? !1;
  }
  mount(c) {
    const e = "#" + CSS.escape(
      c.substring(1)
    ), o = document.querySelector(
      e
    );
    if (!o) {
      console.error(
        "Container not found:",
        e
      );
      return;
    }
    if (o.insertAdjacentHTML(
      "afterbegin",
      this._getTemplate()
    ), setTimeout(() => {
      var u;
      const s = o.querySelector(
        "label"
      );
      s && ((u = s.textContent) != null && u.toLowerCase().includes("onlinebanktransfer")) && (s.textContent = "Online bank transfer");
    }, 100), this.showPayButton) {
      const s = document.querySelector(
        "#onlinebanktransfer-paymentButton"
      );
      s && s.addEventListener(
        "click",
        (u) => {
          u.preventDefault(), this.submit();
        }
      );
    }
  }
  async submit() {
    this.sdk.init({
      environment: this.environment
    });
    const c = window.location.pathname.split("/")[1], o = new URL(window.location.href).origin;
    try {
      const s = {
        paymentMethod: {
          type: "ONLINE_BANK_TRANSFER"
        },
        paymentOutcome: H.AUTHORIZED,
        lang: c ?? "de",
        path: o
      }, u = await fetch(
        this.processorUrl + "/redirectPayment",
        {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            "X-Session-Id": this.sessionId
          },
          body: JSON.stringify(
            s
          )
        }
      );
      if (!u.ok) {
        const h = await u.text();
        throw console.error(
          "HTTP error response:",
          h
        ), new Error(
          `HTTP error! status: ${u.status}`
        );
      }
      const b = await u.json();
      b != null && b.txnSecret ? window.location.href = b.txnSecret : (console.error(
        "Missing txnSecret:",
        b
      ), this.onError(
        "Payment redirect failed."
      ));
    } catch (s) {
      console.error(
        "Online bank transfer error:",
        s
      ), this.onError(
        "Some error occurred. Please try again."
      );
    }
  }
  _getTemplate() {
    const c = document.documentElement.lang || "en", e = c.startsWith("de") ? "Bezahlen Sie bequem per Online-Überweisung." : "Pay easily with Online Bank Transfer.";
    return `
      <div
        id="novalnet-onlinebanktransfer-wrapper"
        class="${O.wrapper}"
      >

        <p>
          ${e}
        </p>

        ${this.showPayButton ? `
            <button
              class="${E.button}
              ${E.fullWidth}
              ${O.submitButton}"

              type="button"

              id="onlinebanktransfer-paymentButton"
            >
              ${c.startsWith("de") ? "Bezahlen" : "Pay Now"}
            </button>
            ` : ""}

      </div>
    `;
  }
}
class ct {
  constructor(d) {
    P(this, "componentHasSubmit", !0);
    this.baseOptions = d;
  }
  build(d) {
    return new lt(this.baseOptions, d);
  }
}
class lt extends M {
  constructor(c, e) {
    super(q.alipay, c, e);
    P(this, "showPayButton");
    this.showPayButton = (e == null ? void 0 : e.showPayButton) ?? !1;
  }
  mount(c) {
    document.querySelector(c).insertAdjacentHTML("afterbegin", this._getTemplate()), this.showPayButton && document.querySelector("#purchaseOrderForm-paymentButton").addEventListener("click", (e) => {
      e.preventDefault(), this.submit();
    });
  }
  async submit() {
    this.sdk.init({ environment: this.environment });
    const c = window.location.pathname.split("/")[1], o = new URL(window.location.href).origin;
    try {
      const s = {
        paymentMethod: {
          type: "ALIPAY"
        },
        paymentOutcome: H.AUTHORIZED,
        lang: c ?? "de",
        path: o
      }, u = await fetch(this.processorUrl + "/redirectPayment", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "X-Session-Id": this.sessionId
        },
        body: JSON.stringify(s)
      });
      if (!u.ok) {
        const h = await u.text();
        throw new Error(`HTTP error! status: ${u.status}`);
      }
      const b = await u.json();
      window.location.href = b.txnSecret;
    } catch (s) {
      console.error("=== PAYMENT SUBMISSION ERROR ===:", s), console.error("Error details:", {
        message: s.message,
        stack: s.stack,
        name: s.name
      }), this.onError("Some error occurred. Please try again.");
    }
  }
  _getTemplate() {
    return this.showPayButton ? `
    <div class="${O.wrapper}">
      <p>Pay easily with Alipay and transfer the shopping amount within the specified date.</p>
      <button class="${E.button} ${E.fullWidth} ${O.submitButton}" id="purchaseOrderForm-paymentButton">Pay Now</button>
    </div>
    ` : "";
  }
}
class dt {
  constructor(d) {
    P(this, "componentHasSubmit", !0);
    this.baseOptions = d;
  }
  build(d) {
    return new pt(this.baseOptions, d);
  }
}
class pt extends M {
  constructor(c, e) {
    super(q.bancontact, c, e);
    P(this, "showPayButton");
    this.showPayButton = (e == null ? void 0 : e.showPayButton) ?? !1;
  }
  mount(c) {
    document.querySelector(c).insertAdjacentHTML("afterbegin", this._getTemplate()), this.showPayButton && document.querySelector("#purchaseOrderForm-paymentButton").addEventListener("click", (e) => {
      e.preventDefault(), this.submit();
    });
  }
  async submit() {
    this.sdk.init({ environment: this.environment });
    const c = window.location.pathname.split("/")[1], o = new URL(window.location.href).origin;
    try {
      const s = {
        paymentMethod: {
          type: "BANCONTACT"
        },
        paymentOutcome: H.AUTHORIZED,
        lang: c ?? "de",
        path: o
      }, u = await fetch(this.processorUrl + "/redirectPayment", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "X-Session-Id": this.sessionId
        },
        body: JSON.stringify(s)
      });
      if (!u.ok) {
        const h = await u.text();
        throw new Error(`HTTP error! status: ${u.status}`);
      }
      const b = await u.json();
      window.location.href = b.txnSecret;
    } catch (s) {
      console.error("Error details:", {
        message: s.message,
        stack: s.stack,
        name: s.name
      }), this.onError("Some error occurred. Please try again.");
    }
  }
  _getTemplate() {
    return this.showPayButton ? `
    <div class="${O.wrapper}">
      <p>Pay easily with Bancontact and transfer the shopping amount within the specified date.</p>
      <button class="${E.button} ${E.fullWidth} ${O.submitButton}" id="purchaseOrderForm-paymentButton">Pay Now</button>
    </div>
    ` : "";
  }
}
class ft {
  constructor(d) {
    P(this, "componentHasSubmit", !0);
    this.baseOptions = d;
  }
  build(d) {
    return new yt(this.baseOptions, d);
  }
}
class yt extends M {
  constructor(c, e) {
    super(q.blik, c, e);
    P(this, "showPayButton");
    this.showPayButton = (e == null ? void 0 : e.showPayButton) ?? !1;
  }
  mount(c) {
    document.querySelector(c).insertAdjacentHTML("afterbegin", this._getTemplate()), this.showPayButton && document.querySelector("#purchaseOrderForm-paymentButton").addEventListener("click", (e) => {
      e.preventDefault(), this.submit();
    });
  }
  async submit() {
    this.sdk.init({ environment: this.environment });
    const c = window.location.pathname.split("/")[1], o = new URL(window.location.href).origin;
    try {
      const s = {
        paymentMethod: {
          type: "BLIK"
        },
        paymentOutcome: H.AUTHORIZED,
        lang: c ?? "de",
        path: o
      }, u = await fetch(this.processorUrl + "/redirectPayment", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "X-Session-Id": this.sessionId
        },
        body: JSON.stringify(s)
      });
      if (!u.ok) {
        const h = await u.text();
        throw new Error(`HTTP error! status: ${u.status}`);
      }
      const b = await u.json();
      window.location.href = b.txnSecret;
    } catch (s) {
      console.error("Error details:", {
        message: s.message,
        stack: s.stack,
        name: s.name
      }), this.onError("Some error occurred. Please try again.");
    }
  }
  _getTemplate() {
    return this.showPayButton ? `
    <div class="${O.wrapper}">
      <p>Pay easily with Blik and transfer the shopping amount within the specified date.</p>
      <button class="${E.button} ${E.fullWidth} ${O.submitButton}" id="purchaseOrderForm-paymentButton">Pay Now</button>
    </div>
    ` : "";
  }
}
class Tt {
  constructor(d) {
    P(this, "componentHasSubmit", !0);
    this.baseOptions = d;
  }
  build(d) {
    return new mt(this.baseOptions, d);
  }
}
class mt extends M {
  constructor(c, e) {
    super(q.eps, c, e);
    P(this, "showPayButton");
    this.showPayButton = (e == null ? void 0 : e.showPayButton) ?? !1;
  }
  mount(c) {
    document.querySelector(c).insertAdjacentHTML("afterbegin", this._getTemplate()), this.showPayButton && document.querySelector("#purchaseOrderForm-paymentButton").addEventListener("click", (e) => {
      e.preventDefault(), this.submit();
    });
  }
  async submit() {
    this.sdk.init({ environment: this.environment });
    const c = window.location.pathname.split("/")[1], o = new URL(window.location.href).origin;
    try {
      const s = {
        paymentMethod: {
          type: "EPS"
        },
        paymentOutcome: H.AUTHORIZED,
        lang: c ?? "de",
        path: o
      }, u = await fetch(this.processorUrl + "/redirectPayment", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "X-Session-Id": this.sessionId
        },
        body: JSON.stringify(s)
      });
      if (!u.ok) {
        const h = await u.text();
        throw console.error("HTTP error response:", h), new Error(`HTTP error! status: ${u.status}`);
      }
      const b = await u.json();
      window.location.href = b.txnSecret;
    } catch (s) {
      console.error("=== PAYMENT SUBMISSION ERROR ===:", s), console.error("Error details:", {
        message: s.message,
        stack: s.stack,
        name: s.name
      }), this.onError("Some error occurred. Please try again.");
    }
  }
  _getTemplate() {
    return this.showPayButton ? `
    <div class="${O.wrapper}">
      <p>Pay easily with Eps and transfer the shopping amount within the specified date.</p>
      <button class="${E.button} ${E.fullWidth} ${O.submitButton}" id="purchaseOrderForm-paymentButton">Pay Now</button>
    </div>
    ` : "";
  }
}
class bt {
  constructor(d) {
    P(this, "componentHasSubmit", !0);
    this.baseOptions = d;
  }
  build(d) {
    return new wt(
      this.baseOptions,
      d
    );
  }
}
class wt extends M {
  constructor(c, e) {
    super(
      q.mbway,
      c,
      e
    );
    P(this, "showPayButton");
    P(this, "isSubmitting", !1);
    this.showPayButton = (e == null ? void 0 : e.showPayButton) ?? !1;
  }
  mount(c) {
    const e = "#" + CSS.escape(
      c.substring(1)
    ), o = document.querySelector(
      e
    );
    if (!o) {
      console.error(
        "Container not found:",
        e
      );
      return;
    }
    if (o.insertAdjacentHTML(
      "afterbegin",
      this._getTemplate()
    ), setTimeout(() => {
      var u;
      const s = o.querySelector(
        "label"
      );
      s && ((u = s.textContent) != null && u.toLowerCase().includes("mbway")) && (s.textContent = "MB Way");
    }, 100), this.showPayButton) {
      const s = document.querySelector(
        "#mbway-paymentButton"
      );
      s && s.addEventListener(
        "click",
        (u) => {
          u.preventDefault(), this.submit();
        }
      );
    }
  }
  async submit() {
    this.sdk.init({
      environment: this.environment
    });
    const c = window.location.pathname.split("/")[1], o = new URL(window.location.href).origin;
    try {
      const s = {
        paymentMethod: {
          type: "MBWAY"
        },
        paymentOutcome: H.AUTHORIZED,
        lang: c ?? "de",
        path: o
      }, u = await fetch(
        this.processorUrl + "/redirectPayment",
        {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            "X-Session-Id": this.sessionId
          },
          body: JSON.stringify(
            s
          )
        }
      );
      if (!u.ok) {
        const h = await u.text();
        throw console.error(
          "HTTP error response:",
          h
        ), new Error(
          `HTTP error! status: ${u.status}`
        );
      }
      const b = await u.json();
      b != null && b.txnSecret ? window.location.href = b.txnSecret : (console.error(
        "Missing redirect URL:",
        b
      ), this.onError(
        "Redirect URL missing."
      ));
    } catch (s) {
      console.error(
        "MB Way payment error:",
        s
      ), this.onError(
        "Some error occurred. Please try again."
      );
    }
  }
  _getTemplate() {
    const c = document.documentElement.lang || "en", e = c.startsWith("de") ? "Bezahlen Sie bequem mit MB Way." : "Pay easily with MB Way.";
    return `
      <div
        id="novalnet-mbway-wrapper"
        class="${O.wrapper}"
      >

        <p>
          ${e}
        </p>

        ${this.showPayButton ? `
            <button
              class="${E.button}
              ${E.fullWidth}
              ${O.submitButton}"

              id="mbway-paymentButton"
              type="button"
            >
              ${c.startsWith("de") ? "Bezahlen" : "Pay Now"}
            </button>
            ` : ""}

      </div>
    `;
  }
}
class ht {
  constructor(d) {
    P(this, "componentHasSubmit", !0);
    this.baseOptions = d;
  }
  build(d) {
    return new Pt(this.baseOptions, d);
  }
}
class Pt extends M {
  constructor(c, e) {
    super(q.multibanco, c, e);
    P(this, "showPayButton");
    this.showPayButton = (e == null ? void 0 : e.showPayButton) ?? !1;
  }
  mount(c) {
    document.querySelector(c).insertAdjacentHTML("afterbegin", this._getTemplate()), this.showPayButton && document.querySelector("#purchaseOrderForm-paymentButton").addEventListener("click", (e) => {
      e.preventDefault(), this.submit();
    });
  }
  async submit() {
    this.sdk.init({ environment: this.environment });
    const c = window.location.pathname.split("/")[1], o = new URL(window.location.href).origin;
    try {
      const s = {
        paymentMethod: {
          type: "MULTIBANCO"
        },
        paymentOutcome: H.AUTHORIZED,
        lang: c ?? "de",
        path: o
      }, b = await (await fetch(this.processorUrl + "/directPayment", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "X-Session-Id": this.sessionId
        },
        body: JSON.stringify(s)
      })).json();
      b.paymentReference ? this.onComplete && this.onComplete({
        isSuccess: !0,
        paymentReference: b.paymentReference
      }) : this.onError("Some error occurred. Please try again.");
    } catch (s) {
      console.error("Error details:", {
        message: s.message,
        stack: s.stack,
        name: s.name
      }), this.onError("Some error occurred. Please try again.");
    }
  }
  _getTemplate() {
    return this.showPayButton ? `
    <div class="${O.wrapper}">
      <p>Pay easily with Multibanco and transfer the shopping amount within the specified date.</p>
      <button class="${E.button} ${E.fullWidth} ${O.submitButton}" id="purchaseOrderForm-paymentButton">Pay Now</button>
    </div>
    ` : "";
  }
}
class St {
  constructor(d) {
    P(this, "componentHasSubmit", !0);
    this.baseOptions = d;
  }
  build(d) {
    return new Bt(
      this.baseOptions,
      d
    );
  }
}
let Bt = class extends M {
  constructor(c, e) {
    super(
      q.postfinance,
      c,
      e
    );
    P(this, "showPayButton");
    this.showPayButton = (e == null ? void 0 : e.showPayButton) ?? !1;
  }
  mount(c) {
    const e = "#" + CSS.escape(
      c.substring(1)
    ), o = document.querySelector(
      e
    );
    if (!o) {
      console.error(
        "Container not found:",
        e
      );
      return;
    }
    if (o.insertAdjacentHTML(
      "afterbegin",
      this._getTemplate()
    ), setTimeout(() => {
      var u;
      const s = o.querySelector(
        "label"
      );
      s && ((u = s.textContent) != null && u.toLowerCase().includes("postfinance")) && (s.textContent = "PostFinance E-Finance");
    }, 100), this.showPayButton) {
      const s = document.querySelector(
        "#purchaseOrderForm-paymentButton"
      );
      s && s.addEventListener(
        "click",
        (u) => {
          u.preventDefault(), this.submit();
        }
      );
    }
  }
  async submit() {
    this.sdk.init({
      environment: this.environment
    });
    const c = window.location.pathname.split("/")[1], o = new URL(window.location.href).origin;
    try {
      const s = {
        paymentMethod: {
          type: "POSTFINANCE_EFINANCE"
        },
        paymentOutcome: H.AUTHORIZED,
        lang: c ?? "de",
        path: o
      }, u = await fetch(
        this.processorUrl + "/redirectPayment",
        {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            "X-Session-Id": this.sessionId
          },
          body: JSON.stringify(
            s
          )
        }
      );
      if (!u.ok) {
        const h = await u.text();
        throw console.error(
          "HTTP error response:",
          h
        ), new Error(
          `HTTP error! status: ${u.status}`
        );
      }
      const b = await u.json();
      window.location.href = b.txnSecret;
    } catch (s) {
      console.error(
        "PostFinance E-Finance payment error:",
        s
      ), this.onError(
        "Some error occurred. Please try again."
      );
    }
  }
  _getTemplate() {
    const c = document.documentElement.lang || "en", e = c.startsWith("de") ? "Bezahlen Sie bequem mit PostFinance E-Finance." : "Pay easily with PostFinance E-Finance.";
    return this.showPayButton ? `
      <div class="${O.wrapper}">

        <p>
          ${e}
        </p>

        <button
          class="${E.button}
          ${E.fullWidth}
          ${O.submitButton}"
          id="purchaseOrderForm-paymentButton"
        >
          ${c.startsWith("de") ? "Bezahlen" : "Pay Now"}
        </button>

      </div>
      ` : "";
  }
};
class vt {
  constructor(d) {
    P(this, "componentHasSubmit", !0);
    this.baseOptions = d;
  }
  build(d) {
    return new gt(
      this.baseOptions,
      d
    );
  }
}
class gt extends M {
  constructor(c, e) {
    super(
      q.postfinance,
      c,
      e
    );
    P(this, "showPayButton");
    P(this, "isSubmitting", !1);
    this.showPayButton = (e == null ? void 0 : e.showPayButton) ?? !1;
  }
  mount(c) {
    const e = "#" + CSS.escape(
      c.substring(1)
    ), o = document.querySelector(
      e
    );
    if (!o) {
      console.error(
        "Container not found:",
        e
      );
      return;
    }
    if (o.insertAdjacentHTML(
      "afterbegin",
      this._getTemplate()
    ), setTimeout(() => {
      var u;
      const s = o.querySelector(
        "label"
      );
      s && ((u = s.textContent) != null && u.toLowerCase().includes("postfinancecard")) && (s.textContent = "PostFinance Card");
    }, 100), this.showPayButton) {
      const s = document.querySelector(
        "#purchaseOrderForm-paymentButton"
      );
      s && s.addEventListener(
        "click",
        (u) => {
          u.preventDefault(), this.submit();
        }
      );
    }
  }
  async submit() {
    this.sdk.init({
      environment: this.environment
    });
    const c = window.location.pathname.split("/")[1], o = new URL(window.location.href).origin;
    try {
      const s = {
        paymentMethod: {
          type: "POSTFINANCE"
        },
        paymentOutcome: H.AUTHORIZED,
        lang: c ?? "de",
        path: o
      }, u = await fetch(
        this.processorUrl + "/redirectPayment",
        {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            "X-Session-Id": this.sessionId
          },
          body: JSON.stringify(
            s
          )
        }
      );
      if (!u.ok) {
        const h = await u.text();
        throw console.error(
          "HTTP error response:",
          h
        ), new Error(
          `HTTP error! status: ${u.status}`
        );
      }
      const b = await u.json();
      b != null && b.txnSecret ? window.location.href = b.txnSecret : (console.error(
        "Missing redirect URL:",
        b
      ), this.onError(
        "Redirect URL missing."
      ));
    } catch (s) {
      console.error(
        "PostFinance payment error:",
        s
      ), this.onError(
        "Some error occurred. Please try again."
      );
    }
  }
  _getTemplate() {
    const c = document.documentElement.lang || "en", e = c.startsWith("de") ? "Bezahlen Sie bequem mit PostFinance Card." : "Pay easily with PostFinance Card.";
    return `
      <div
        id="novalnet-postfinance-wrapper"
        class="${O.wrapper}"
      >

        <p>
          ${e}
        </p>

        ${this.showPayButton ? `
            <button
              class="${E.button}
              ${E.fullWidth}
              ${O.submitButton}"

              id="postfinance-paymentButton"
              type="button"
            >
              ${c.startsWith("de") ? "Bezahlen" : "Pay Now"}
            </button>
            ` : ""}

      </div>
    `;
  }
}
class Et {
  constructor(d) {
    P(this, "componentHasSubmit", !0);
    this.baseOptions = d;
  }
  build(d) {
    return new Ot(this.baseOptions, d);
  }
}
class Ot extends M {
  constructor(c, e) {
    super(q.przelewy24, c, e);
    P(this, "showPayButton");
    this.showPayButton = (e == null ? void 0 : e.showPayButton) ?? !1;
  }
  mount(c) {
    document.querySelector(c).insertAdjacentHTML("afterbegin", this._getTemplate()), this.showPayButton && document.querySelector("#purchaseOrderForm-paymentButton").addEventListener("click", (e) => {
      e.preventDefault(), this.submit();
    });
  }
  async submit() {
    this.sdk.init({ environment: this.environment });
    const c = window.location.pathname.split("/")[1], o = new URL(window.location.href).origin;
    try {
      const s = {
        paymentMethod: {
          type: "PRZELEWY24"
        },
        paymentOutcome: H.AUTHORIZED,
        lang: c ?? "de",
        path: o
      }, u = await fetch(this.processorUrl + "/redirectPayment", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "X-Session-Id": this.sessionId
        },
        body: JSON.stringify(s)
      });
      if (!u.ok) {
        const h = await u.text();
        throw new Error(`HTTP error! status: ${u.status}`);
      }
      const b = await u.json();
      window.location.href = b.txnSecret;
    } catch (s) {
      console.error("Error details:", {
        message: s.message,
        stack: s.stack,
        name: s.name
      }), this.onError("Some error occurred. Please try again.");
    }
  }
  _getTemplate() {
    return this.showPayButton ? `
    <div class="${O.wrapper}">
      <p>Pay easily with Przelewy24 and transfer the shopping amount within the specified date.</p>
      <button class="${E.button} ${E.fullWidth} ${O.submitButton}" id="purchaseOrderForm-paymentButton">Pay Now</button>
    </div>
    ` : "";
  }
}
class Ct {
  constructor(d) {
    P(this, "componentHasSubmit", !0);
    this.baseOptions = d;
  }
  build(d) {
    return new It(this.baseOptions, d);
  }
}
class It extends M {
  constructor(c, e) {
    super(q.trustly, c, e);
    P(this, "showPayButton");
    this.showPayButton = (e == null ? void 0 : e.showPayButton) ?? !1;
  }
  mount(c) {
    document.querySelector(c).insertAdjacentHTML("afterbegin", this._getTemplate()), this.showPayButton && document.querySelector("#purchaseOrderForm-paymentButton").addEventListener("click", (e) => {
      e.preventDefault(), this.submit();
    });
  }
  async submit() {
    this.sdk.init({ environment: this.environment });
    const c = window.location.pathname.split("/")[1], o = new URL(window.location.href).origin;
    try {
      const s = {
        paymentMethod: {
          type: "TRUSTLY"
        },
        paymentOutcome: H.AUTHORIZED,
        lang: c ?? "de",
        path: o
      }, u = await fetch(this.processorUrl + "/redirectPayment", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "X-Session-Id": this.sessionId
        },
        body: JSON.stringify(s)
      });
      if (!u.ok) {
        const h = await u.text();
        throw new Error(`HTTP error! status: ${u.status}`);
      }
      const b = await u.json();
      window.location.href = b.txnSecret;
    } catch (s) {
      console.error("Error details:", {
        message: s.message,
        stack: s.stack,
        name: s.name
      }), this.onError("Some error occurred. Please try again.");
    }
  }
  _getTemplate() {
    return this.showPayButton ? `
    <div class="${O.wrapper}">
      <p>Pay easily with Trustly and transfer the shopping amount within the specified date.</p>
      <button class="${E.button} ${E.fullWidth} ${O.submitButton}" id="purchaseOrderForm-paymentButton">Pay Now</button>
    </div>
    ` : "";
  }
}
class Ut {
  constructor(d) {
    P(this, "componentHasSubmit", !0);
    this.baseOptions = d;
  }
  build(d) {
    return new Rt(this.baseOptions, d);
  }
}
class Rt extends M {
  constructor(c, e) {
    super(q.twint, c, e);
    P(this, "showPayButton");
    this.showPayButton = (e == null ? void 0 : e.showPayButton) ?? !1;
  }
  mount(c) {
    document.querySelector(c).insertAdjacentHTML("afterbegin", this._getTemplate()), this.showPayButton && document.querySelector("#purchaseOrderForm-paymentButton").addEventListener("click", (e) => {
      e.preventDefault(), this.submit();
    });
  }
  async submit() {
    this.sdk.init({ environment: this.environment });
    const c = window.location.pathname.split("/")[1], o = new URL(window.location.href).origin;
    try {
      const s = {
        paymentMethod: {
          type: "TWINT"
        },
        paymentOutcome: H.AUTHORIZED,
        lang: c ?? "de",
        path: o
      }, u = await fetch(this.processorUrl + "/redirectPayment", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "X-Session-Id": this.sessionId
        },
        body: JSON.stringify(s)
      });
      if (!u.ok) {
        const h = await u.text();
        throw new Error(`HTTP error! status: ${u.status}`);
      }
      const b = await u.json();
      window.location.href = b.txnSecret;
    } catch (s) {
      console.error("=== PAYMENT SUBMISSION ERROR ===:", s), console.error("Error details:", {
        message: s.message,
        stack: s.stack,
        name: s.name
      }), this.onError("Some error occurred. Please try again.");
    }
  }
  _getTemplate() {
    return this.showPayButton ? `
    <div class="${O.wrapper}">
      <p>Pay easily with Twint and transfer the shopping amount within the specified date.</p>
      <button class="${E.button} ${E.fullWidth} ${O.submitButton}" id="purchaseOrderForm-paymentButton">Pay Now</button>
    </div>
    ` : "";
  }
}
class Lt {
  constructor(d) {
    P(this, "componentHasSubmit", !0);
    this.baseOptions = d;
  }
  build(d) {
    return new Nt(this.baseOptions, d);
  }
}
class Nt extends M {
  constructor(c, e) {
    super(q.wechatpay, c, e);
    P(this, "showPayButton");
    this.showPayButton = (e == null ? void 0 : e.showPayButton) ?? !1;
  }
  mount(c) {
    document.querySelector(c).insertAdjacentHTML("afterbegin", this._getTemplate()), this.showPayButton && document.querySelector("#purchaseOrderForm-paymentButton").addEventListener("click", (e) => {
      e.preventDefault(), this.submit();
    });
  }
  async submit() {
    this.sdk.init({ environment: this.environment });
    const c = window.location.pathname.split("/")[1], o = new URL(window.location.href).origin;
    try {
      const s = {
        paymentMethod: {
          type: "WECHATPAY"
        },
        paymentOutcome: H.AUTHORIZED,
        lang: c ?? "de",
        path: o
      }, u = await fetch(this.processorUrl + "/redirectPayment", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "X-Session-Id": this.sessionId
        },
        body: JSON.stringify(s)
      });
      if (!u.ok) {
        const h = await u.text();
        throw new Error(`HTTP error! status: ${u.status}`);
      }
      const b = await u.json();
      window.location.href = b.txnSecret;
    } catch (s) {
      console.error("=== PAYMENT SUBMISSION ERROR ===:", s), console.error("Error details:", {
        message: s.message,
        stack: s.stack,
        name: s.name
      }), this.onError("Some error occurred. Please try again.");
    }
  }
  _getTemplate() {
    return this.showPayButton ? `
    <div class="${O.wrapper}">
      <p>Pay easily with Wechatpay and transfer the shopping amount within the specified date.</p>
      <button class="${E.button} ${E.fullWidth} ${O.submitButton}" id="purchaseOrderForm-paymentButton">Pay Now</button>
    </div>
    ` : "";
  }
}
class kt {
  constructor(d) {
    P(this, "componentHasSubmit", !0);
    this.baseOptions = d;
  }
  build(d) {
    return new $t(
      this.baseOptions,
      d
    );
  }
}
class $t extends M {
  constructor(c, e) {
    super(
      q.sepa,
      c,
      e
    );
    P(this, "showPayButton");
    this.showPayButton = (e == null ? void 0 : e.showPayButton) ?? !1;
  }
  mount(c) {
    const e = "#" + CSS.escape(
      c.substring(1)
    ), o = document.querySelector(
      e
    );
    if (!o) {
      console.error(
        "Container not found:",
        e
      );
      return;
    }
    if (o.insertAdjacentHTML(
      "afterbegin",
      this._getTemplate()
    ), setTimeout(() => {
      var u;
      const s = o.querySelector(
        "label"
      );
      s && ((u = s.textContent) != null && u.toLowerCase().includes("sepa")) && (s.textContent = "Direct Debit SEPA");
    }, 100), this.showPayButton) {
      const s = document.querySelector(
        "#sepa-payment-button"
      );
      s && s.addEventListener(
        "click",
        (u) => {
          u.preventDefault(), this.submit();
        }
      );
    }
  }
  async submit() {
    var s, u, b;
    this.sdk.init({
      environment: this.environment
    });
    const c = window.location.pathname.split("/")[1], o = new URL(window.location.href).origin;
    try {
      const h = document.getElementById(
        "nn_account_holder"
      ), Q = document.getElementById(
        "nn_sepa_account_no"
      ), x = document.getElementById(
        "nn_sepa_bic"
      ), ee = ((s = h == null ? void 0 : h.value) == null ? void 0 : s.trim()) ?? "", te = ((u = Q == null ? void 0 : Q.value) == null ? void 0 : u.trim()) ?? "", Y = ((b = x == null ? void 0 : x.value) == null ? void 0 : b.trim()) ?? "";
      if (!ee) {
        this.onError(
          "Please enter account holder name"
        );
        return;
      }
      if (!te) {
        this.onError(
          "Please enter IBAN"
        );
        return;
      }
      const J = {
        paymentMethod: {
          type: "DIRECT_DEBIT_SEPA",
          accHolder: ee,
          iban: te,
          bic: Y
        },
        paymentOutcome: H.AUTHORIZED,
        lang: c ?? "de",
        path: o
      }, Z = await fetch(
        this.processorUrl + "/directPayment",
        {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            "X-Session-Id": this.sessionId
          },
          body: JSON.stringify(
            J
          )
        }
      );
      if (!Z.ok) {
        const ue = await Z.text();
        throw console.error(
          "HTTP error response:",
          ue
        ), new Error(
          `HTTP error! status: ${Z.status}`
        );
      }
      const oe = await Z.json();
      console.log(
        "SEPA payment response:",
        oe
      ), oe.paymentReference ? this.onComplete && this.onComplete({
        isSuccess: !0,
        paymentReference: oe.paymentReference
      }) : this.onError(
        "Some error occurred. Please try again."
      );
    } catch (h) {
      console.error(
        "SEPA submit error:",
        {
          message: h == null ? void 0 : h.message,
          stack: h == null ? void 0 : h.stack
        }
      ), this.onError(
        "Some error occurred. Please try again."
      );
    }
  }
  _getTemplate() {
    const c = this.showPayButton ? `
          <button
            class="
              ${E.button}
              ${E.fullWidth}
              ${O.submitButton}
            "

            id="sepa-payment-button"

            type="button"
          >
            Pay Now
          </button>
        ` : "";
    return `

      <div
        class="${O.wrapper}"

        style="
          width:100%;
          display:flex;
          flex-direction:column;
          gap:20px;
          margin-top:20px;
        "
      >

        <p>
          Pay conveniently using
          Direct Debit SEPA.
        </p>

        <div
          id="nn_sepa_form"

          style="
            width:100%;
            display:flex;
            flex-direction:column;
            gap:20px;
          "
        >

          <!-- Account Holder -->
          <div
            style="
              display:flex;
              flex-direction:column;
              width:100%;
            "
          >

            <label
              for="nn_account_holder"

              style="
                font-size:14px;
                font-weight:600;
                color:#333;
                margin-bottom:6px;
              "
            >
              Account Holder
              <span style="color:red;">
                *
              </span>
            </label>

            <input
              type="text"

              id="nn_account_holder"

              name="nn_account_holder"

              autocomplete="off"

              style="
                padding:12px 14px;
                border:1px solid #d4d4d4;
                border-radius:6px;
                font-size:15px;
              "
            />
          </div>

          <!-- IBAN -->
          <div
            style="
              display:flex;
              flex-direction:column;
              width:100%;
            "
          >

            <label
              for="nn_sepa_account_no"

              style="
                font-size:14px;
                font-weight:600;
                color:#333;
                margin-bottom:6px;
              "
            >
              IBAN
              <span style="color:red;">
                *
              </span>
            </label>

            <input
              type="text"

              id="nn_sepa_account_no"

              name="nn_sepa_account_no"

              autocomplete="off"

              style="
                padding:12px 14px;
                border:1px solid #d4d4d4;
                border-radius:6px;
                font-size:15px;
                text-transform:uppercase;
              "
            />
          </div>

          <!-- BIC -->
          <div
            style="
              display:flex;
              flex-direction:column;
              width:100%;
            "
          >

            <label
              for="nn_sepa_bic"

              style="
                font-size:14px;
                font-weight:600;
                color:#333;
                margin-bottom:6px;
              "
            >
              BIC
            </label>

            <input
              type="text"

              id="nn_sepa_bic"

              name="nn_sepa_bic"

              autocomplete="off"

              style="
                padding:12px 14px;
                border:1px solid #d4d4d4;
                border-radius:6px;
                font-size:15px;
              "
            />
          </div>

          ${c}

        </div>

      </div>
    `;
  }
}
class At {
  constructor(d) {
    P(this, "componentHasSubmit", !0);
    this.baseOptions = d;
  }
  build(d) {
    return new jt(
      this.baseOptions,
      d
    );
  }
}
class jt extends M {
  constructor(c, e) {
    super(
      q.ach,
      c,
      e
    );
    P(this, "showPayButton");
    this.showPayButton = (e == null ? void 0 : e.showPayButton) ?? !1;
  }
  mount(c) {
    const e = "#" + CSS.escape(
      c.substring(1)
    ), o = document.querySelector(
      e
    );
    if (!o) {
      console.error(
        "Container not found:",
        e
      );
      return;
    }
    if (o.insertAdjacentHTML(
      "afterbegin",
      this._getTemplate()
    ), setTimeout(() => {
      var u;
      const s = o.querySelector(
        "label"
      );
      s && ((u = s.textContent) != null && u.toLowerCase().includes("ach")) && (s.textContent = "Direct Debit ACH");
    }, 100), this.showPayButton) {
      const s = document.querySelector(
        "#achForm-paymentButton"
      );
      s && s.addEventListener(
        "click",
        (u) => {
          u.preventDefault(), this.submit();
        }
      );
    }
  }
  async submit() {
    var s;
    this.sdk.init({
      environment: this.environment
    });
    const c = window.location.pathname.split("/")[1], o = new URL(window.location.href).origin;
    try {
      const u = document.getElementById(
        "achForm-accHolder"
      ), b = document.getElementById(
        "achForm-accountNumber"
      ), h = document.getElementById(
        "achForm-routingNumber"
      ), Q = (u == null ? void 0 : u.value.trim()) ?? "", x = (b == null ? void 0 : b.value.trim()) ?? "", ee = (h == null ? void 0 : h.value.trim()) ?? "";
      if (!Q || !x || !ee) {
        this.onError(
          "Please fill all required fields."
        );
        return;
      }
      const te = {
        paymentMethod: {
          type: "DIRECT_DEBIT_ACH",
          accHolder: Q,
          accountNumber: x,
          routingNumber: ee
        },
        paymentOutcome: H.AUTHORIZED,
        lang: c ?? "en",
        path: o
      };
      console.log(
        "ACH Request:",
        te
      );
      const Y = await fetch(
        this.processorUrl + "/directPayment",
        {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            "X-Session-Id": this.sessionId
          },
          body: JSON.stringify(
            te
          )
        }
      );
      if (!Y.ok) {
        const Z = await Y.text();
        throw console.error(
          "HTTP error response:",
          Z
        ), new Error(
          `HTTP error! status: ${Y.status}`
        );
      }
      const J = await Y.json();
      if (console.log(
        "ACH Response:",
        J
      ), J && J.paymentReference) {
        const Z = typeof J.paymentReference == "string" ? J.paymentReference : J.paymentReference.id;
        console.log(
          "Calling onComplete with:",
          Z
        ), (s = this.onComplete) == null || s.call(this, {
          isSuccess: !0,
          paymentReference: Z
        });
      } else
        console.error(
          "Missing paymentReference:",
          J
        ), this.onError(
          "Payment reference missing."
        );
    } catch (u) {
      console.error(
        "ACH payment error:",
        u
      ), this.onError(
        "Some error occurred. Please try again."
      );
    }
  }
  _getTemplate() {
    const c = document.documentElement.lang || "en";
    return `
    <div class="${O.wrapper}">

      <div
        style="
          display:flex;
          flex-direction:column;
          gap:16px;
        "
      >

        <!-- Account Holder -->
        <div>

          <label for="achForm-accHolder">  
            ${c.startsWith("de") ? "Kontoinhaber" : "Account Holder"}
          </label>

          <input
            type="text"
            id="achForm-accHolder"
            name="accHolder"

            style="
              width:100%;
              padding:12px;
              margin-top:6px;
            "
          />
        </div>

        <!-- Account Number -->
        <div>

          <label for="achForm-accountNumber">
            ${c.startsWith("de") ? "Kontonummer" : "Account Number"}
          </label>

          <input
            type="text"
            id="achForm-accountNumber"
            name="accountNumber"

            style="
              width:100%;
              padding:12px;
              margin-top:6px;
            "
          />

        </div>

        <!-- Routing Number -->
        <div>

          <label for="achForm-routingNumber">
            ${c.startsWith("de") ? "Bankleitzahl" : "Routing Number"}
          </label>

          <input
            type="text"
            id="achForm-routingNumber"
            name="routingNumber"

            style="
              width:100%;
              padding:12px;
              margin-top:6px;
            "
          />

        </div>

        ${this.showPayButton ? `
            <button
              class="${E.button}
              ${E.fullWidth}
              ${O.submitButton}"

              id="achForm-paymentButton"
            >
              ${c.startsWith("de") ? "Bezahlen" : "Pay"}
            </button>
            ` : ""}

      </div>

    </div>
    `;
  }
}
class _t {
  constructor(d) {
    P(this, "componentHasSubmit", !0);
    this.baseOptions = d;
  }
  build(d) {
    return new Ft(
      this.baseOptions,
      d
    );
  }
}
class Ft extends M {
  constructor(c, e) {
    super(
      q.creditcard,
      c,
      e
    );
    P(this, "showPayButton");
    this.showPayButton = (e == null ? void 0 : e.showPayButton) ?? !1;
  }
  mount(c) {
    const e = "#" + CSS.escape(
      c.substring(1)
    ), o = document.querySelector(
      e
    );
    if (!o) {
      console.error(
        "Container not found:",
        e
      );
      return;
    }
    if (o.querySelector(
      "#novalnet_iframe"
    ))
      return;
    o.insertAdjacentHTML(
      "beforeend",
      this._getTemplate()
    ), setTimeout(async () => {
      if (!document.getElementById(
        "novalnet_iframe"
      )) {
        console.error(
          "Novalnet iframe not found"
        );
        return;
      }
      const b = document.querySelector(
        "#purchaseOrderForm-paymentButton"
      );
      try {
        await this._loadNovalnetScriptOnce(), await this._initNovalnetCreditCardForm(
          b
        );
      } catch (h) {
        console.error(
          "Failed to initialize Novalnet form:",
          h
        );
      }
    }, 500);
    const s = document.querySelector(
      "#purchaseOrderForm-paymentButton"
    );
    this.showPayButton && s && !s._nnBound && (s._nnBound = !0, s.addEventListener(
      "click",
      async (u) => {
        u.preventDefault();
        const b = window.NovalnetUtility;
        if (b != null && b.getPanHash)
          try {
            s.disabled = !0, await b.getPanHash();
          } catch (h) {
            s.disabled = !1, console.error(
              "Error getting pan hash:",
              h
            );
          }
        else
          console.warn(
            "NovalnetUtility.getPanHash not available"
          );
      }
    ));
  }
  async submit() {
    var s;
    this.sdk.init({
      environment: this.environment
    });
    const c = window.location.pathname.split("/")[1], o = new URL(window.location.href).origin;
    try {
      const u = document.getElementById(
        "pan_hash"
      ), b = document.getElementById(
        "unique_id"
      ), h = document.getElementById(
        "do_redirect"
      ), Q = u == null ? void 0 : u.value.trim(), x = b == null ? void 0 : b.value.trim(), ee = h == null ? void 0 : h.value.trim();
      if (!Q || !x) {
        this.onError(
          "Credit card information is missing or invalid."
        );
        return;
      }
      const te = {
        paymentMethod: {
          type: "CREDITCARD",
          panHash: Q,
          uniqueId: x,
          doRedirect: ee
        },
        paymentOutcome: H.AUTHORIZED,
        lang: c ?? "de",
        path: o
      }, Y = await fetch(
        this.processorUrl + "/directPayment",
        {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            "X-Session-Id": this.sessionId
          },
          body: JSON.stringify(
            te
          )
        }
      );
      if (!Y.ok) {
        const Z = await Y.text();
        throw console.error(
          "HTTP error:",
          Z
        ), new Error(
          `HTTP error ${Y.status}`
        );
      }
      const J = await Y.json();
      J.paymentReference ? (s = this.onComplete) == null || s.call(this, {
        isSuccess: !0,
        paymentReference: J.paymentReference
      }) : this.onError(
        "Payment failed. Please try again."
      );
    } catch (u) {
      console.error(
        "Payment submit error:",
        u
      ), this.onError(
        "Some error occurred. Please try again."
      );
    }
  }
  _getTemplate() {
    const c = this.showPayButton ? `
          <button
            class="
              ${E.button}
              ${E.fullWidth}
              ${O.submitButton}
            "

            id="purchaseOrderForm-paymentButton"

            type="button"
          >
            Pay
          </button>
        ` : "";
    return `
      <div class="${O.wrapper}">

        <iframe
          id="novalnet_iframe"
          frameborder="0"
          scrolling="no"
          style="
            min-width: 40%;
            border:none;
            display:block;
          "
        ></iframe>

        <input
          type="hidden"
          id="pan_hash"
          name="pan_hash"
        />

        <input
          type="hidden"
          id="unique_id"
          name="unique_id"
        />

        <input
          type="hidden"
          id="do_redirect"
          name="do_redirect"
        />

        ${c}

      </div>
    `;
  }
  async _loadNovalnetScriptOnce() {
    if (window.NovalnetUtility)
      return;
    const c = "https://cdn.novalnet.de/js/v2/NovalnetUtility-1.1.2.js";
    if (document.querySelector(
      `script[src="${c}"]`
    ))
      return;
    const o = document.createElement(
      "script"
    );
    o.src = c, o.crossOrigin = "anonymous";
    const s = new Promise(
      (u, b) => {
        o.onload = () => u(), o.onerror = (h) => b(h);
      }
    );
    document.head.appendChild(
      o
    ), await s;
  }
  async _initNovalnetCreditCardForm(c) {
    var b;
    const e = window.NovalnetUtility;
    if (!e) {
      console.warn(
        "NovalnetUtility not available."
      );
      return;
    }
    const s = await (await fetch(
      this.processorUrl + "/getconfig",
      {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          Accept: "application/json"
        },
        body: JSON.stringify({
          paymentMethod: {
            type: "CREDITCARD"
          },
          paymentOutcome: "AUTHORIZED"
        })
      }
    )).json();
    if (!(s != null && s.paymentReference))
      throw new Error(
        "Missing client key"
      );
    this.clientKey = String(
      s.paymentReference
    ), e.setClientKey(
      this.clientKey
    );
    const u = {
      callback: {
        on_success: async (h) => (document.getElementById(
          "pan_hash"
        ).value = h.hash, document.getElementById(
          "unique_id"
        ).value = h.unique_id, document.getElementById(
          "do_redirect"
        ).value = h.do_redirect, c && (c.disabled = !1), await this.submit(), !0),
        on_error: (h) => (h != null && h.error_message && alert(
          h.error_message
        ), c && (c.disabled = !1), !1)
      },
      iframe: {
        id: "novalnet_iframe",
        inline: 1,
        style: {
          container: "",
          input: "",
          label: ""
        },
        text: {
          lang: (b = window.location.pathname.split("/")[1]) == null ? void 0 : b.toUpperCase(),
          card_holder: {
            label: "Card holder name",
            place_holder: "Name on card"
          },
          card_number: {
            label: "Card number",
            place_holder: "XXXX XXXX XXXX XXXX"
          },
          expiry_date: {
            label: "Expiry date"
          },
          cvc: {
            label: "CVC/CVV/CID",
            place_holder: "XXX"
          }
        }
      }
    };
    e.createCreditCardForm(
      u
    );
  }
}
class Ht {
  /**
   * Creates an instance of FakeSdk.
   * @param environment - The environment for the SDK.
   */
  constructor({ environment: d }) {
    P(this, "environment");
    this.environment = d, console.log("FakeSdk constructor", this.environment);
  }
  /**
   * Initializes the SDK with the specified options.
   * @param opts - The options for initializing the SDK.
   */
  init(d) {
    console.log("FakeSdk init", d);
  }
}
const Ie = class Ie {
  constructor(d) {
    P(this, "setupData");
    this.setupData = Ie._Setup(d);
  }
  async createComponentBuilder(d) {
    const { baseOptions: c } = await this.setupData, e = d == null ? void 0 : d.trim().toLowerCase();
    let o = e;
    e.includes("ideal") && (o = "ideal"), (e.includes("direct debit sepa") || e.includes("sepa") || e.includes("Direct Debit SEPA")) && (o = "sepa"), (e.includes("direct debit ach") || e.includes("ach") || e.includes("Direct Debit ACH")) && (o = "ach"), (e.includes("online bank transfer") || e.includes("onlinebanktransfer") || e.includes("Online bank transfer")) && (o = "onlinebanktransfer"), (e.includes("credit/debit cards") || e.includes("creditcard") || e.includes("Credit/Debit Cards")) && (o = "creditcard"), (e.includes("mb way") || e.includes("mbway") || e.includes("MB Way")) && (o = "mbway"), (e.includes("invoice with payment guarantee") || e.includes("GuaranteedInvoice") || e.includes("Invoice with payment guarantee")) && (o = "GuaranteedInvoice"), (e.includes("direct debit sepa with payment guarantee") || e.includes("GuaranteedSepa") || e.includes("Direct Debit SEPA with payment guarantee")) && (o = "GuaranteedSepa"), (e.includes("postFinance card") || e.includes("postfinancecard") || e.includes("PostFinance Card")) && (o = "postfinancecard"), (e.includes("postFinance e-finance") || e.includes("postfinance") || e.includes("PostFinance E-Finance")) && (o = "postfinance");
    const u = {
      invoice: Je,
      prepayment: Ze,
      guaranteedinvoice: Qe,
      guaranteedsepa: tt,
      ideal: rt,
      paypal: ot,
      onlinebanktransfer: st,
      alipay: ct,
      bancontact: dt,
      blik: ft,
      eps: Tt,
      mbway: bt,
      multibanco: ht,
      postfinance: St,
      postfinancecard: vt,
      przelewy24: Et,
      trustly: Ct,
      twint: Ut,
      wechatpay: Lt,
      sepa: kt,
      ach: At,
      creditcard: _t
    }[o];
    if (!u)
      throw new Error(`Unsupported payment type: ${d}`);
    return new u(c);
  }
};
P(Ie, "_Setup", async (d) => {
  const c = {
    // environment: configJson.environment,
    environment: "test"
  };
  return Promise.resolve({
    baseOptions: {
      sdk: new Ht(c),
      processorUrl: d.processorUrl,
      sessionId: d.sessionId,
      environment: c.environment,
      onComplete: d.onComplete || (() => {
      }),
      onError: d.onError || (() => {
      })
    }
  });
});
let Ae = Ie;
export {
  Ae as Enabler
};
